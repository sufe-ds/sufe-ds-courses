# C++ 语言程序设计基础类和对象

# 目录

基本概念

类和对象的定义

类的继承和派生

类成员和派生类的访问控制

类成员函数的多态

结构体

# 面向对象方法

- 将数据及对数据的操作方法封装在一起，作为一个相互依存、不可分离的整体——对象。

- 对同类型对象抽象出其共性，形成类。

- 类通过一个简单的外部接口，与外界发生关系。

- 优点:

□程序模块间的关系更为简单，程序模块的独立性、数据的安全性就有了良好的保障。

□通过继承与多态性，可以大大提高程序的可重用性，使得软件的开发和维护都更为方便。

# 面向对象方法的基本概念——对象

- 一般意义上的对象:

□是现实世界中一个实际存在的事物。

□可以是有形的（比如一辆汽车），也可以是无形的（比如一项计划）

- 面向对象方法中的对象:

□是系统中用来描述客观事物的一个实体，它是用来构成系统的一个基本单位。对象由一组属性和一组行为构成。

□属性：用来描述对象静态特征的数据项。

□行为：用来描述对象动态特征的操作序列。

# 面向对象方法的基本概念——类

- 分类——人类通常的思维方法

- 分类所依据的原则——抽象

□忽略事物的非本质特征，只注意那些与当前目标有关的本质特征，从而找出事物的共性，把具有共同性质的事物划分为一类，得出一个抽象的概念

□例如，石头、树木、汽车、房屋等都是人们在长期的生产和生活实践中抽象出的概念。

- 面向对象方法中的“类”

□具有相同属性和服务的一组对象的集合

□为属于该类的全部对象提供了抽象的描述，包括属性和行为两个主要部分

□类与对象的关系:

犹如模具与铸件之间的关系，一个属于某类的对象称为该类的一个实例。

# 抽象

- 对同一类对象的共同属性和行为进行概括，形成类。

□ 先注意问题的本质，其次是实现过程或细节。

□数据抽象：描述某类对象的属性或状态（对象相互区别的物理量）。

□ 代码抽象：描述某类对象的共有的行为特征或具有的功能。

□ 抽象的实现：类。

□例如：学生类

# 封装

- 把对象的属性和服务结合成一个独立的系统单元。

- 尽可能隐蔽对象的内部细节。对外形成一个边界（或者说一道屏障），只保留有限的对外接口使之与外部发生联系。

□ 目的：增强安全性和简化编程，使用者不必了解具体的实现细节，而只需要通过外部接口，以特定的访问权限，来使用类的成员。

□ 实现封装：类声明中的  $\{\}$

□例如：学生类

# 继承

- 继承对于软件复用有着重要意义，是面向对象技术能够提高软件开发效率的重要原因之一。

• 定义：特殊类的对象拥有其一般类的全部属性与服务，称作特殊类对一般类的继承。

• 例如：将图形作为一个一般类，圆形便是一个特殊类。

• 又如: 人 - 学生

# 多态

- 多态是指在一般类中定义的属性或行为，被特殊类继承之后，可以具有不同的数据类型或表现出不同的行为。这使得同一个属性或行为在一般类及其各个特殊类中具有不同的语义。

例如:

□图形类：计算图形面积  $\rightarrow$  圆类计算圆面积

-> 矩形类计算机型面积

# 目录

基本概念

类和对象的定义

类的继承和派生

类成员和派生类的访问控制

类成员函数的多态

结构体

# 类定义的语法形式

```txt
class 类名称
{
public:
    公有成员（外部接口）
private:
    私有成员
protected:
    保护型成员
}
```

# 例——钟表类

class Clock {

public:

voidSETS(int newH, int newM, int newS);

void showTime();

private:

int hour, minute, second;

}；

# 例——钟表类

![](file://~/MinerU/绪论2-C++的类和对象.pptx-05944e63-4cf6-418e-98dc-dcedb2f2a8bc/images/e1919b2546ea48a8c33cf9b0749e099e26d058c7c7473e936e896925d6630e7d.jpg)


# 对象和构造函数

- 对象是类的一个实例

- 构造函数：在对象被创建时使用特定的值构造对象，将对象初始化为一个特定的初始状态。

例如:

□希望在构造一个 Clock 类对象时，将初试时间设为 0:0:0，就可以通过构造函数来设置。

# 例：通过构造函数创建对象

// 类定义

class Clock {

public:

Clock(int newH, int newM, int newS); // 构造函数

voidSETS(int newH, int newM, int newS);

void showTime();

private:

int hour, minute, second;

};

# 例：通过构造函数创建对象

// 构造函数的实现:

```autohotkey
Clock::Clock(int newH, int newM, int newS): hour(newH), minute(newM), second(newS) {
}
```

```javascript
int main() { Clock c(0,0,0); // 自动调用构造函数  
c.showTime();  
return 0;
```

# 构造函数的形式

- 函数名与类名相同;

- 不能定义返回值类型，也不能有 return 语句；

- 可以有形式参数，也可以没有形式参数；

- 可以是内联函数;

- 可以重载;

- 可以带默认参数值。

# 构造函数的调用时机

- 在对象创建时被自动调用

例如:

Clock myClock (0,0,0);

# 默认构造函数

- 调用时可以不需要实参的构造函数

□参数表为空的构造函数

全部参数都有默认值的构造函数

下面两个都是默认构造函数，如在类中同时出现，将产生编译错误：

Clock();

Clock(int newH = 0, int newM = 0, int newS = 0);

# 析构函数

完成对象被删除前的一些清理工作。

- 在对象的生存期结束的时刻系统自动调用它，然后再释放此对象所属的空间。

- 如果程序中未声明析构函数，编译器将自动产生一个默认的析构函数，其函数体为空。

# 例：析构函数

include<iostream>

using namespace std;

class Point {

public:

Point(int xx, int yy);

$\sim$  Point();

//...其他函数原型

private:

int x, y;

};

Point::Point(int xx, int yy)

{

$$
x = x x;
$$

$$
y = y y;
$$

}

Point::~Point() {

}

//...其他函数的实现略

# 类的成员函数

- 在类中说明函数原型;

- 可以在类外给出函数体实现，并在函数名前使用类名加以限定；

- 也可以直接在类中给出函数体，形成内联成员函数；

# 例：类的成员函数

```txt
include<iostream> using namespace std;
```

```txt
// 类的定义：在类内仅声明成员函数原型  
class Student {  
public:
```

```txt
//1. 成员函数原型原型声明（无函数体，仅说明函数名、参数、返回值）  
void setInfo(string name, int age); //设置学生信息  
void showInfo(); //显示学生信息
```

```txt
private: string stuName; //私有成员变量：学生姓名 intstuAge; //私有成员变量：学生年龄 };
```

```cpp
//2.类外实现成员函数：需用“类名::”限定函数所属的类  
void Student::setInfo(string name,int age){//函数体：给私有成员变量赋值stuName  $\equiv$  name;stuAge  $\equiv$  age;  
}  
//类外实现另一个成员函数  
void Student::showInfo(){//函数体：输出学生信息cout << "姓名：" << stuName << ",年龄：" << stuAge << endl;
```

# 内联成员函数

- 为了提高运行时的效率，对于较简单的函数可以声明为内联形式。

- 内联函数的本质：编译器会尝试将“函数调用”替换为“函数体代码”，避免函数调用的开销（如栈帧创建、参数传递）

- 内联函数体中不要有复杂结构（如循环语句和 switch 语句）。

在类中声明内联成员函数的方式:

□ 将函数体放在类的声明中。

□ 使用 inline 关键字。

# 内联成员函数（例）

```cpp
include <string>   
using namespace std;   
class Student {   
private: string name; int age;   
public: //显式加inline inlineStudent(stringn,inta){ name  $\equiv$  n;age  $\equiv$  a;} inline string getName(){ return name; }   
}；
```

# 目录

基本概念

类和对象的定义

类的继承和派生

类成员和派生类的访问控制

类成员函数的多态

结构体

# 类的继承和派生的概念

# 继承和派生的概念

□ 继承是指一个类（派生类 / 子类）从另一个类（基类 / 父类）获取成员（变量、函数）的机制，派生则指创建子类的过程

# - 基类 (父类)

□ 被继承的原始类, 包含通用的属性和行为 (如 “人” 类包含 “姓名、年龄” 和 “吃饭、睡觉”);

# - 派生类（子类）：

□ 基于基类创建的新类, 可: 继承基类的非 private 成员 (public/protected) ; 添加自身特有的成员（如 “学生” 类新增 “学号、选课”）; 重写基类的函数（如 “人” 的 “工作” 函数, “学生” 重写为 “学习”）;

# 继承方式:

□ 控制基类成员在派生类中的访问权限，分 public、protected、private 三种（最常用 public）。

# 派生类的定义

(单继承时)

class派生类名：继承方式基类名 成员声明;

例如：  
class Derived: public Base{public:Derived();\~Derived();}；

继承方式:

public

□ private

protected

# 例：人（基类）- 学生（派生类）

include<iostream>

include <string>

using namespace std;

// 基类：人（Person）—通用属性和行为

class Person {

public:

// 基类成员：通用属性

string name;

int age;

// 基类成员：通用行为

void eat() {

cout << name << " ( " << age << "岁) 在吃饭" << endl;

}

// 基类构造函数：初始化通用属性

Person(string n, int a): name(n), age(a) {}};

//派生类：学生（Student）—public继承Person，扩展特有功能

class Student : public Person {

public:

//派生类特有属性：学号

string student_id;

//派生类特有行为：选课

void choose_course(string course) {cout << name << " (学号: " << student_id << ") 选了 < " << course << ") << endl; }

//派生类构造函数：先初始化基类（必须），再初始化自身属性

Student(string n, int a, string id): Person(n, a), student_id(id) {}

}；

int main() {

// 创建派生类对象：学生小明

Student xiao_ming("小明", 18, "2025001");

//1.使用继承的基类成员（代码复用）

xiao_ming.eat(); //调用基类的eat()函数

xiao_ming.age = 19; // 修改基类的age属性

cout << "小明更新年龄：" << xiao_ming.age << "岁" << endl;

//2.使用派生类特有成员（功能扩展）

xiao_mingchoose_course("C++编程");

}

# 目录

基本概念

类和对象的定义

类的继承和派生

类成员和派生类的访问控制

类成员函数的多态

结构体

# 类成员的访问控制

# - Private 类型成员

“类内可访问”:

该类内部定义的所有成员函数，都能直接读写 private 变量、调用 private 成员函数，不受任何限制。

# 例：类内可访问 Private 成员

include<iostream>

include <string>

using namespace std;

class Person {

private:

// private 变量：姓名（仅类内可直接访问）

string name;

// private 成员函数：仅类内可直接调用

void formatName(string& n) {

n[0] = toupper(n[0]); // 首字母大写

(辅助逻辑, 仅类内使用) \}

public:

// 类内 public 成员函数：可直接访问 private 成员

Person(string n) {

formatName(n); //调用private成员函数

name = n; // 赋值 private 变量

}

void showName() const {

cout << " 姓名: " << name << endl; // 读取

private 变量

}

};

# 类成员的访问控制

# - Private 类型成员

# “类外全禁止”:

该类的对象无法访问：即使是类的实例，在类外部也不能用“对象名.变量名”、“对象名.函数名”直接操作 private 成员;

子类无法访问：子类（派生类）虽继承父类，但父类的 private 成员对子类完全隐藏，子类的成员函数也不能直接访问；

其他类 / 全局函数无法直接访问：非该类的外部代码（包括其他类的成员函数、全局函数），无任何直接访问权限。

# 例：类外不可访问 Private 成员

include<iostream>

using namespace std;

class Parent {

private:

int private_val = 10; // 父类 private 变量

};

// 子类：无法访问父类的 private 成员

class Child : public Parent {

public:

void accessParent() {

//cout << private_val; //错误：父类private成

员，子类不可访问

}

};

// 其他类：无法访问 Parent 的 private 成员

class OtherClass {

public:

void accessParent(Parent& p) {

//cout<<p(private_val; //错误：外部类无法

访问 private 成员

}；

// 全局函数：无法访问 Parent 的 private 成员  
void globalFunc(Parent& p) {

//cout << p(private_val; //错误：全局函数无法

访问 private 成员

}

int main() {

Parent p;

//cout << p(private_val; //错误：类对象在类

外，无法访问 private 成员

return 0;}

# 类成员的访问控制

# - Protected 类型成员

□类内可访问：定义 protected 成员的类（基类）自身的成员函数，可直接访问该 protected 成员；

□子类可访问：继承该基类的子类（派生类）的成员函数，可直接访问基类的protected成员;

□类外不可访问：无论是基类的对象、子类的对象，在类外部（如 main 函数）都无法直接访问 protected 成员。

# 例：类内可访问 Protected 成员

```cpp
include<iostream>   
#include<string>   
using namespace std;   
//基类：人（定义protected成员）   
class Person{   
protected: string name; void setName(const string&n){ name  $\equiv$  n; }   
private: string idCard;   
public: void showBaseInfo() const { cout << "姓名：" << name << endl; //类内可访问protected成员}   
};
```

# 例：子类成员函数可访问基类 Protected 成员

// 子类：学生（继承 Person）

class Student : public Person {

private:

int studentId;

public:

// 子类构造函数：访问基类的 protected 成员

Student(const string& n, int id): studentId(id) {

// 子类成员函数可直接访问基类的 protected 成员

name = n; // 直接修改基类的 protected 变量

// setName(n); // 也可直接调用基类的 protected 成员函数 }

// 子类成员函数：复用基类的 protected 成员

void showStudentInfo() const {

// 直接访问基类的 protected 成员 name

cout << "学生姓名：" << name << ", 学号：" << studentId << endl;

//cout << idCard; //错误：基类的private成员，子类也不能访问

}   
};

# 例：类外不可访问 Protected 成员

# // 类外部（main 函数）

```txt
int main() {
```

// 1. 基类对象：类外不能访问 protected 成员

```txt
Person p;
```

// p.name = " 张三 "; // 错误: protected 成员, 类外不可访问

// p.setName("张三"); // 错误：protected 成员函数，类外不可调用

pshowBaseInfo(); // 合法：通过 public 接口间接访问

// 2. 子类对象：类外也不能访问基类的 protected 成员

Student s(" 李四 ", 2025001);

// s.name = "王五 "; // 错误: 子类对象在类外, 不能访问基类的 protected 成员

s.showStudentInfo(); // 合法：通过子类的 public 接口间接访问

sshowBaseInfo(); // 合法：继承基类的 public 接口

```txt
return 0;
```

```txt
}
```

# 类成员的访问控制

# Public 类型成员

□类内可访问：与 Private/Protected 相同

□类外可访问:

□ 类外部的所有代码（包括全局函数、其他类的成员函数），都能通过“对象名.成员名”（非静态成员）或“类名：：成员名”（静态成员）直接访问public成员，无需通过中间接口。

(注: 静态成员不依赖具体实例, 被所有该类的实例共享)

# 例：类外访问 Public 成员

```cpp
include<iostream> #include<string> using namespace std;
```

```txt
//定义类：包含public成员   
class Book{   
public: //public变量：书名、价格（外部可直接读写） string title; double price;   
//public成员函数：外部可直接调用 void printInfo() const { cout<<"书名：" <<title<<"，价格：" << endl; }   
}；
```

```javascript
//1.全局函数访问Book的public成员void setBookInfo(Book&book, const string& title,double price){book.title  $=$  title; //外部函数直接修改public变量book.price  $=$  price;
```

# 类成员访问控制总结

<table><tr><td>成员权限修饰符</td><td>类内部(自身成员函数)</td><td>类外部(通过对象访问)</td><td>派生类内部(子类成员函数)</td><td>核心特点与用途</td></tr><tr><td>public</td><td>可访问</td><td>可访问</td><td>可访问</td><td>对外暴露的接口(如通用功能、属性),是类与外部交互的通道,如 print() 函数、name 属性</td></tr><tr><td>protected</td><td>可访问</td><td>不可访问</td><td>可访问</td><td>类内部与派生类共享的成员(不对外暴露),如父类的内部状态 battery (电量),保证继承复用的同时隐藏实现</td></tr><tr><td>private</td><td>可访问</td><td>不可访问</td><td>不可访问</td><td>类完全封装的内部成员(仅自身可用),如 health_status (健康状态),是类的“私有边界”,只能通过 public/protected 接口间接访问</td></tr></table>

# 友元函数

- 友元函数是在类声明中由关键字 friend 修饰说明的非成员函数（本质上是全局函数），在它的函数体中能够通过对象名访问 private 和 protected 成员

- 作用：增加灵活性，使程序员可以在封装和快速性方面做合理选择。

# 例：使用友元函数计算两点间的距离

include<iostream>

include <math>

class Point { //Point 类声明

public://外部接口

Point(int x=0, int y=0): x(x), y(y) {}

int getX() { return x; }

int getY() { return y; }

friend float dist(Point &a, Point &b);

private: //私有数据成员

int x, y;

};

# 例：使用友元函数计算两点间的距离

```cpp
float dist( Point& a, Point& b) {
    double x = a.x - b.x;
    double y = a.y - b.y;
    return static_cast< float >(sqrt(x * x + y * y));
}
int main() {
    Point p1(1, 1), p2(4, 5);
    cout << "The distance is: ";
    cout << dist(p1, p2) << endl;
    return 0;
} 运行结果：
```

运行结果:

The distance is: 5

# 回顾派生类的定义

(单继承时)

class派生类名：继承方式基类名{成员声明;

例如：  
class Derived: public Base{public:Derived();\~Derived();}；

继承方式:

public

□ private

protected

# 派生类的访问控制：public 继承

# - 关键特性

□派生类与基类构成“is-a”关系（例如“学生 is-a 人”），派生类对象可视为基类对象使用（如赋值给基类指针 / 引用）；

# - 权限传递规则

□基类的 public 成员  $\rightarrow$  派生类的 public 成员 (权限不变) ;

□基类的 protected 成员  $\rightarrow$  派生类的 protected 成员 (权限不变) ;

□基类的 private 成员  $\rightarrow$  派生类不可直接访问（权限隔绝）。

# 例: public 继承的访问控制

```txt
include<iostream> using namespace std;
```

// 基类：图形（Shape）

```txt
class Shape{
```

```txt
public:
```

```txt
void show_type() { // public成员：派生类/外部可访问  
cout << "这是一个图形" << endl;  
}
```

```txt
protected:
```

```txt
double area; // protected成员：仅基类/派生类可访问
```

```txt
private:
```

```txt
int color; // private成员：仅基类可访问
```

```txt
public:
```

```c
void set_color(int c) { color = c; } // 接口间接访问private
```

```txt
int main() {  
    Circle c;  
    c.calc_area(2); // 调用派生类函数  
    c.print_area();  
    c.show_type(); // 外部直接访问基类public成员（权限传递）  
    // c.area = 5; // 错误：基类protected→派生类protected，外部不可访问  
    return 0;  
}
```

```cpp
//派生类：圆形（Circle）-public继承Shape（圆形是一种图形）  
class Circle : public Shape{public:void calc_area(double r){area  $= 3.14^{*}r^{*}r$  ；//访问基类protected成员（权限传递）show_type(); //访问基类public成员（权限传递）//color  $= 1$  ；//错误：基类private成员不可访问set_color(2); //通过基类接口间接用private成员}void print_area(){cout << "圆形面积：" << area << endl;1};
```

# is-a 关系：“圆形是一种图形”

权限传递:

基类 public（show_type()） $\rightarrow$  派生类 public，外部可访问；

基类 protected（area） $\rightarrow$ 派生类 protected，仅内部可访问；

基类 private（color） $\rightarrow$ 派生类不可直接访问，需通过基类接口（set_color()）间接使用。

# 派生类的访问控制：protected 继承

# - 关键特性

□外部派生类对象无法直接访问基类传递来的任何成员（原 public 成员已变为 protected，仅派生类内部可访问）

# - 权限传递规则

□基类的 public 成员  $\rightarrow$  派生类的 protected 成员 (权限降低);

□基类的 protected 成员  $\rightarrow$  派生类的 protected 成员 (权限不变) ;

□基类的 private 成员  $\rightarrow$  派生类不可直接访问（权限隔绝）。

# 例: protected 继承的访问控制

```txt
include<iostream>   
using namespace std;   
//基类：设备（Device）   
class Device{   
public: void power_on() { // public成员：控制开机 cout << "设备开机" << endl; }   
protected: int battery; // protected成员：电量（内部属性）   
private: bool is_repaired; // private成员：维修状态   
public: void set_repaired(bool r){is_repaired  $\equiv$  r;} //接口访问private   
};
```

```javascript
int main(){SmartDevice sd;sd.start();//仅能通过派生类public接口使用基类功能//sd.power_on();//错误：基类public  $\rightarrow$  派生类protected，外部不可访问Phonep;p.boot();//间接派生类可继续使用基类传递的成员//pbattery  $= 50$  ；//错误：protected成员外部不可访问return0;1
```

```cpp
//派生类：智能设备（SmartDevice）-protected继承Device   
classSmartDevice:protected Device{   
public: //派生类对外接口：间接使用基类功能   
void start(){ power_on();//访问基类public  $\rightarrow$  派生类protected（内部可访问） battery  $= 100$  ;//访问基类protected  $\rightarrow$  派生类protected（内部可访问） set_repaired(true);//调用基类public接口 cout << "智能设备启动，电量："<<battery<<endl; }   
}；
```

# 权限传递:

基类 public（power_on()） $\rightarrow$ 派生类 protected，仅内部 / 子类可访问；

基类 protected（battery） $\rightarrow$ 派生类 protected，内部/子类可访问；

基类 private（is_repaired） $\rightarrow$  始终不可直接访问，需通过接口。

# 派生类的访问控制：private 继承

# - 关键特性

□派生类与基类构成“has-a”关系（例如“汽车 has-a 发动机”），更接近“组合”的效果（派生类内部使用基类的功能，外部完全不可见）；

# - 权限传递规则

□基类的 public 成员  $\rightarrow$  派生类的 private 成员 (权限降至最低) ;

□基类的 protected 成员  $\rightarrow$  派生类的 private 成员 (权限降至最低) ;

□基类的 private 成员  $\rightarrow$  派生类不可直接访问（权限隔绝）。

# 派生类的访问控制总结

<table><tr><td>继承方式</td><td>基类成员权限</td><td>派生类内部(成员函数中)</td><td>派生类外部(通过对象)</td><td>间接派生类内部(子类的子类)</td></tr><tr><td>public 继承</td><td>public</td><td>✓</td><td>✓</td><td>✓</td></tr><tr><td></td><td>protected</td><td>✓</td><td>X</td><td>✓</td></tr><tr><td></td><td>private</td><td>X</td><td>X</td><td>X</td></tr><tr><td>protected 继承</td><td>public</td><td>✓</td><td>X</td><td>✓</td></tr><tr><td></td><td>protected</td><td>✓</td><td>X</td><td>✓</td></tr><tr><td></td><td>private</td><td>X</td><td>X</td><td>X</td></tr><tr><td>private 继承</td><td>public</td><td>✓</td><td>X</td><td>X</td></tr><tr><td></td><td>protected</td><td>✓</td><td>X</td><td>X</td></tr><tr><td></td><td>private</td><td>X</td><td>X</td><td>X</td></tr></table>

# 目录

基本概念

类和对象的定义

类的继承和派生

类成员和派生类的访问控制

类成员函数的多态

结构体

# 多态

- 多态：同一名称，不同的功能实现方式。

• 目的：达到行为标识统一，减少程序中标识符的个数。

- 实现：虚函数、函数重载等

# 例：通过虚函数实现运行时多态

```txt
include<iostream> using namespace std;
```

```cpp
class Base1 {
public:
    virtual void display() const; // 虚函数
};
void Base1::display() const {
    cout << "Base1::display()" << endl;
}
```

# 例：通过虚函数实现运行时多态

```cpp
class Base2:public Base1{ public: virtual void display() const; }; void Base2::display() const { cout << "Base2::display(" << endl;   
} class Derived:public Base2{ public: virtual void display() const; }; void Derived::display() const { cout << "Derived::display(" << endl;
```

# 例：通过虚函数实现运行时多态

```txt
void fun(Base1 \*ptr) { ptr->display(); }
```

```txt
int main() { Base1 base1; Base2 base2; Derived derived; fun(&base1); fun(&base2); fun(&derived); return 0; }
```

运行结果:

Base1::display()

Base2::display()

Derived::display()

# 目录

基本概念

类和对象的定义

类的继承和派生

类成员和派生类的访问控制

类成员函数的多态

结构体

# 结构体

- 结构体是一种特殊形态的类

□与类的唯一区别：类的缺省访问权限是private，结构体的缺省访问权限是public

□结构体存在的主要原因：与C语言保持兼容

- 什么时候用结构体而不用类

□定义主要用来保存数据、而没有什么操作的类型

口人们习惯将结构体的数据成员设为公有，因此这时用结构体更方便

# 结构体的定义

# - 结构体定义

```c
struct 结构体名称 {
    公有成员
protected:
    保护型成员
private:
    私有成员
};
```

# 结构体的初始化

- 如果一个结构体的全部数据成员都是公共成员，并且没有用户定义的构造函数，没有基类和虚函数，这个结构体的变量可以用下面的语法形式赋初值□类型名 变量名 = { 成员数据 1 初值，成员数据 2 初值，……}；

# 例：用结构体表示学生的基本信息

```cpp
include<iostream> #include <iomanip> #include<string> using namespace std;
```

```txt
struct Student { //学生信息结构体  
    int num; //学号  
    string name; //姓名，字符串对象，将在第6章详细介绍  
    char sex; //性别  
    int age; //年龄  
};
```

# 例：用结构体表示学生的基本信息

```txt
int main() { Student stu = {97001, "Lin Lin", 'F', 19}; cout << "Num: " <<stu.num << endl; cout << "Name:" <<stu.name << endl; cout << "Sex:" <<stu_sex << endl; cout << "Age:" <<stu.age << endl; return 0; }
```

运行结果:

Num: 97001

Name: Lin Lin

Sex: F

Age: 19