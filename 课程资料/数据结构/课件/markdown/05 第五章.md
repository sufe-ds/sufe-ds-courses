# 第五章 递归和广义表

- 递归的概念

- 递归转化为非递归

广义表的概念

本章小结

# 5.1 递归的概念

# - 能采用递归解决的问题通常具有这样的特征

- 规模为  $\mathrm{N}$  的问题可以被分解为一些规模较小的问题, 然后从这些小问题的解可以方便地构造出大问题的解。而这些小问题可以同样的方式继续分解成规模更小的问题, 当问题分解到足够小的时候, 例如  $\mathrm{N} = 1$ , 能直接得到解。

一 数据结构可以是递归的。

一 问题的解法可以是递归的。

- 一个过程直接地或间接地调用其自身。最明显可以用递归解决的问题是问题本身的定义是递归的，或者其中的主要数据结构是递归定义的。

- 递归的定义 若一个对象部分地包含它自己，或用它自己给自己定义，则称这个对象是递归的；若一个过程直接地或间接地调用自己，则称这个过程是递归的过程。

以下三种情况常常用到递归方法:

√ 定义是递归的

✓ 数据结构是递归的

问题的解法是递归的

# 定义是递归的

例如，阶乘函数

$$
n! = \left\{ \begin{array}{c c} 1, & \text {当} n = 0 \text {时} \\ n * (n - 1)! , & \text {当} n \geq 1 \text {时} \end{array} \right.
$$

求解阶乘函数的递归算法

long Factorial(long n) {

if  $(n == 0)$  return 1;

else return n*Factorial(n-1);

}

• 递归过程可看作两个过程的组合。

第一个过程是大问题逐渐被分解成小问题，直到递归终止条件，然后开始第二个过程。

一 第二个过程则是从小问题的解合成出大问题的解。

# 求解阶乘  $n!$  的过程

# 递归调用

# 参数传递

主程序 main : fact(4)

参数 4 计算  $4^{*}\mathrm{fact}(3)$  返回 24

参数 3 计算  $3^{*}\mathrm{fact}(2)$  返回 6

参数 2 计算  $2^{*}\mathrm{fact}(1)$  返回 2

参数 1 计算  $1^{*}\mathrm{fact}(0)$  返回 1

参数 0 直接定值  $= 1$  返回 1

# 结果返回

# 回归求值

# 递归的数据结构

# - 很多数据结构都可以用递归的方式描述。

一例如，单链表结构

- 只有一个结点的单链表，是一个单链表；

- 一个普通单链表，可以看做由链表的第一个结点，其指针指向由剩余部分形成的一个单链表。

- 深度为  $n$  的栈也可以描述为由一个深度为  $n - 1$ 的栈和当前栈顶元素构成，而深度为 0 的栈则是空栈。

一链表、树、二叉树都可以类似定义。

# 问题的解法是递归的

例如，汉诺塔 (Tower of Hanoi) 问题的解法

如果  $n = 1$ , 则将这一个盘子直接从 A 柱移到 C 柱上。否则, 执行以下三步:

(1) 用 C 柱做过渡, 将 A 柱上的  $(n - 1)$  个盘子移到 B 柱上;

(2)将  $A_{1}$  柱上最后一个盘子直接移到  $C_{1}$  柱上;

③ 用  $A$  柱做过渡, 将  $B$  柱上的(  $n - 1$ )个盘子移动.

转动条件——两次只能移动一个盘子，

移动过程中，大盘不能放在小盘上面。

# Hanoi 塔问题图示

![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/640cc7d6100159027ee339a26f1d1336e7ae96947db6f3b25615dcc56d4b87c4.jpg)


![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/36d45f5386044dc76788a2fc00adade1ce686fa400acf8e7655e9472376a5922.jpg)


![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/bb717c113c89612578eb26563987f38312b3f98508bf006e5647f20c63e205b8.jpg)


![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/1f82b3207d496a17a0142eb6e8e8c15f851e56efe6cd03eb9b27eff5ddc8bb9d.jpg)


![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/91974af019a71cf017d7187062a689e89204b1e2054f304d2a8f1f4b1b0f9979.jpg)


![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/9d5ccf47062c6da2458f61638d7a52c2231b44cff243c21bdce1558da3555ec5.jpg)


![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/48a5ac833578ed51ccbbf3d6aa7d8567d5afe98cfef31d5910d7cb87abe83895.jpg)


![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/e57746631ec323e2d38aa2c11b5f5d60715e2372c23be7f434f083aa328e9b3c.jpg)


![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/c93e29935f35be6ff208d019fd46c6baede6c7bec4cd89eb5b0c54d2bb59ea50.jpg)


![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/31d9072b785e1815fa8885304f1bbdb655b72455bacea9554be5bac28bb77ee9.jpg)


![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/5633083768568a45a95249705d425fbfdf4fe10c9a5bab326d27abe43a2e277b.jpg)


![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/3ccb5cbe9a2e76e0c5d812c138f06c085c76d2d75011784ddcab8a1b2e283ee6.jpg)


```txt
include<iostream.h>
```

```gradle
include "strclass.h"
```

```txt
void Hanoi(int n, String A, String B, String C) {
```

// 解决汉诺塔问题的递归算法

```javascript
if (n==1)
```

```javascript
cout<<“move”<<A<<“to ”<<C<<endl;
```

```txt
else{
```

```javascript
Hanoi(n-1, A, C, B);
```

```javascript
cout<<“move”<<A<<“to”<<C<<endl;
```

```javascript
Hanoi(n-1, B, A, C);
```

```txt
}
```

```txt
}
```

![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/65ddb082b5c70441e77f637d92385098dd5331332d015f17980c4b7217bb8056.jpg)


# 递归算法的运算时间复杂度分析

- 当  $n = 0$  时,  $\mathrm{T}(n) = 0$ ;

- 当  $n > 0$  时,  $\mathrm{T}(n) = 2 \mathrm{T}(n - 1) + 1$ ;

$$
\begin{array}{l} - \mathrm {T} (n) = 2 \mathrm {T} (n - 1) + 1 = 2 (2 \mathrm {T} (n - 2) + 1) + 1 = \dots \\ = 2 ^ {n - 1} + 2 ^ {n - 2} + \dots + 1 = 2 ^ {n} - 1 \\ \end{array}
$$

# 证明递归程序的正确性

• 递归程序在逻辑上与常见的数学归纳法类似,因此往往可以用数学归纳法来证明递归程序的正确性。在证明的时候, 要证明两个部分。

一个是递归的基础，也就是作为递归终止状态的最基础情况，例如在 Hanoi 塔问题中， $n = 1$  的情况。

- 按照数学归纳法，假设在  $n - 1$  的情况下，Hanoi塔问题的解法是正确的，则由于  $n$  规模的 Hanoi塔问题是通过  $n - 1$  规模的解法解决的，这样就可以很方便地证明 Hanoi 塔问题的递归算法的正确性。

# 自顶向下、逐步分解的策略

- 子问题应与原问题做同样的事情，且更为简单。

- 解决递归问题的策略是把一个规模比较大的问题分解为一个或若干规模比较小的问题，分别对这些比较小的问题求解，再综合它们的结果，从而得到原问题的解。

# 一 分而治之策略（分治法）

- 这些比较小的问题的求解方法与原来问题的求解方法一样。

# 构成递归的条件

- 不能无限制地调用本身，必须有一个出口，化简为非递归状况直接处理。

```c
Procedure <name> (<parameter list>)  
{  
    if (<initial condition>)  
        return (initial value);  
    else  
        return (<name> (parameter exchange));  
}
```

# 递归应用

# - 递归与回溯常用于搜索过程

- 搜索空间通常包含许多结点，且每个结点有多个分支，每次选择一个分支搜索。

- 当搜索到某一结点无法继续搜索下去时，则沿搜索路径回退到前一结点，选择另一分支继续搜索。

如果回退到的结点没有其它选择，则沿搜索路径继续回退，直到找到新的搜索分支，或者搜索到问题的解，或者搜索完全部可搜索的分支没有找到解。

# - 上述问题的特点是将候选解按某种顺序逐一枚举和检验。

- 当发现当前候选解不是解时，就放弃它而选择下一个，这就是回溯。

- 求解这类问题时常常以递归方式（或用栈）向前试探和回溯。

- 之前的迷宫问题是个典型例子。

# 5.2 递归转化为非递归

- 递归方法是一种很有效很自然的分析和描述问题的方法。

- 但递归算法运算效率低，常有重复计算，有时先用递归的思想分析和描述问题，然后转化成非递归的算法。

# 递归过程与递归工作栈

- 递归过程在实现时，需要自己调用自己。

- 层层向下递归，退出时的次序正好相反。

# 递归调用

$$
n! \longrightarrow (n - 1)! \longrightarrow (n - 2)! \longrightarrow 1! \longrightarrow 0! = 1
$$

# 返回次序

- 主程序第一次调用递归过程为外部调用;

- 递归过程每次递归调用自己为内部调用。

- 它们返回调用它的过程的地址不同。

# 递归工作栈

- 每一次递归调用时，需要为过程中使用的参数、局部变量等另外分配存储空间。

- 每层递归调用需分配的空间形成递归工作记录，按后进先出的栈组织。

![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/a39aa25b4e946c900144c6d7867630edcc9cbbf9bb01f094aab775d075637a2b.jpg)


# 函数递归时的活动记录

![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/a1d30395687054c79c2a8948118dc00143131ca64db3deebd0738763f1f5e647.jpg)


返回地址（下一条指令） 局部变量 参数

```javascript
long Factorial(long n) { int temp; if  $(\mathrm{n} = = 0)$  return 1; else temp=n*Factorial(n-1); RetLoc2 return temp; void main(）{ int n; n=Factorial(4); RetLoc1
```

# 计算 Fact 时活动记录的内

# 递归调用序列

参数 返回值 返回地址

返回时的指令

<table><tr><td>4</td><td>24</td><td>RetLoc1</td></tr></table>

return  $4^{*}6 \quad / /$  返回 24

<table><tr><td>3</td><td>6</td><td>RetLoc2</td></tr></table>

return  $3 * 2 \quad / \quad$  返回 6

<table><tr><td>2</td><td>2</td><td>RetLoc2</td></tr></table>

return  $2^{*} 1 \quad / \quad$  返回 2

<table><tr><td>1</td><td>1</td><td>RetLoc2</td></tr></table>

return  $1^{*} 1 \quad / /$  返回 1

<table><tr><td>0</td><td>1</td><td>RetLoc2</td></tr></table>

return 1 // 返回 1

# 递归过程改为非递归过程

- 递归过程简洁、易编、易懂;

- 递归过程效率低，重复计算多；

- 改为非递归过程的目的是提高效率;

单向递归和尾递归可直接用迭代实现其非递归过程;

- 其它情形必须借助栈实现非递归过程。

# 递归转非递归的方法

# - 可以分为两个大的方向

- 一个方向就是寻找与递归算法思路完全不同的非递归算法，这有时候是比较困难的。

- 另一个方向是仍然利用递归的思路，但进行改造。

- 以Hanoi塔问题为例，通过跟踪递归程序的执行过程，可以发现在递归调用前需要保留程序执行的状态，然后为递归调用进行环境的初始化，进而调用递归程序执行，所调用的过程执行完成时，有时候需要传递返回的值，然后恢复调用前保存的执行状态，然后继续执行。

- 因此，最基本的递归转换为非递归的方法就是在非递归算法中实现这些程序执行中间状态的保存和恢复。一般情况下，栈是保存这些中间状态的最理想的数据结构。

# Hanoi 塔问题的递归算法

- void towers(int n, int A, int B, int C)

• {

• if (n==1) printf("\\n%d -> %d\n", A, C);

else

$\bullet$  \{

towers(n-1, A, C, B);

• printf("\\n%d -> %d\n", A, C);

towers(n-1, B, A, C);

$\bullet$

$\bullet$  1

![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/55a4ecdb007396e28a352bfd8e257971d986d89883a04f540e267c3b9adbb6cf.jpg)


# 非递归改造

- 在进行非递归改造时，可参考递归树的遍历次序

# 一 以下算法中

- push 过程把参数加入栈中。

• setvar1(&n, &A, &B, &C) 修改参数，切换为递归树左子树根节点的参数

• setvar2(&n, &A, &B, &C) 修改参数，切换为递归树右子树根节点的参数

# Hanoi 塔问题的非递归算法

// 定义栈元素：保存递归过程中的参数状态

```txt
typedef struct { int n; //盘子数量 int A; //源柱 int B; //辅助柱 int C; //目标柱 } StackElem;
```

//栈结构

```rust
typedef struct {
    StackElem* data; // 存储元素
    int top; // 栈顶指针
    int capacity; // 栈容量
} Stack;
```

//初始化栈

```txt
void initStack(Stack\* s, int capacity) { s->data = (StackElem\*)malloc(sizeof(StackElem) \* capacity); s->top  $= -1$  . s->capacity  $=$  capacity;   
}
```

# Hanoi 塔问题的非递归算法


//压栈


```c
void push(Stack* s, int n, int A, int B, int C) {
    if (s->top < s->capacity - 1) {
        s->data[++s->top] = (StackElem){n, A, B, C};
    }
}
```


//出栈（恢复参数）


```txt
void pop(Stack\* s, int\* n, int\* A, int\* B, int\* C) { if (s->top >= 0) { StackElem elem = s->data[s->top--];  $\star \mathsf{n} =$  elem.n;  $\star \mathsf{A} =$  elem.A;  $\star \mathsf{B} =$  elem.B;  $\star \mathsf{C} =$  elem.C; }
```

# Hanoi 塔问题的非递归算法

// setvar1: 处理“将n-1个盘子从A移到B（辅助C）”，更新参数进入子问题

```c
void setvar1(int\* n, int\* A, int\* B, int\* C) {
```

// 原问题：n个盘子从A→C，辅助B

// 子问题：n-1个盘子从A→B，辅助C（目标变为B，辅助变为C）

```lisp
$(\ast \mathsf{n}) - - ;$
```

// 新的目标柱是B，辅助柱是C，源柱仍为A

```txt
int temp = *C;
```

```lisp
$\star C = \star B;$
```

```txt
*B = temp;
```

```txt
}
```

// setvar2: 处理“将n-1个盘子从B移到C（辅助A）”，更新参数进入子问题

```c
void setvar2(int\* n, int\* A, int\* B, int\* C) {
```

// 原问题：n个盘子从A→C，辅助B（已完成第n个盘子的移动）

// 子问题：n-1个盘子从B→C，辅助A（源柱变为B，辅助变为A）

```lisp
$(\ast \mathsf{n}) - - ;$
```

```txt
int temp = *A;
```

```lisp
$\star A = \star B;$
```

```lisp
$\star B =$  temp;
```

```txt
}
```

# Hanoi 塔问题的非递归算法


// 汉诺塔非递归实现


```c
void towers(int n, int A, int B, int C) {
    Stack s;
}
```


initStack(&s, n); // 栈容量至少为n（递归深度最大为n）


```txt
bool done = false;
```


while (!done) {



// 第一步：将n-1个盘子从A移到B（辅助C），循环压栈保存状态


```javascript
while  $(n > 1)$  {
```


//压入当前状态（后续需处理第n个盘子的移动和剩余子问题）


```javascript
push(&s, n, A, B, C);
```


// 更新参数，进入“n-1个盘子从A→B，辅助C”的子问题


```javascript
setvar1(&n, &A, &B, &C);
```

```txt
}
```


// 第二步：  $n = 1$  时，直接移动最顶层盘子从A到C


```javascript
printf("Move disk 1 from %d to %d\n", A, C);
```

# Hanoi 塔问题的非递归算法

```c
//第三步：恢复栈中状态，处理剩余步骤  
if (!isEmpty(&s)) {  
//弹出上一层状态（n个盘子的原问题）  
pop(&s, &n, &A, &B, &C);  
//移动第n个盘子从A到C（递归中的中间步骤）  
printf("Move disk %d from %d to %d\n", n, A, C);  
//更新参数，进入"n-1个盘子从B→C，辅助A"的子问题  
setvar2(&n, &A, &B, &C);  
} else {  
//栈为空，所有问题处理完毕  
done = true;  
}  
}  
free(s.data); //释放栈内存  
}  
//测试  
int main() {  
printf("汉诺塔移动步骤（3个盘子，柱子1→3，辅助2）：\n");  
towers(3, 1, 2, 3);  
return 0;
```

# 尾递归

# - 递归调用处于递归过程的最后一条语句。

```txt
include<iostream>using namespace std;  
//尾递归计算阶乘：acc为累加器，保存中间结果long long fact_tail(int n,long long acc = 1）{//终止条件：当n=1时返回累加器结果if  $(\mathsf{n} == 1)$  {return acc;1//尾递归调用：更新参数n和累加器accreturn fact_tail(n-1,n\*acc);
```

<table><tr><td>执行步骤</td><td>未开启优化（栈帧堆积）</td><td>开启优化（栈帧复用）</td></tr><tr><td>初始调用</td><td>栈：[(5,1)]</td><td>栈：[(5,1)]</td></tr><tr><td>第1次递归</td><td>栈：[(5,1)，(4,5)]</td><td>栈：[(4,5)]</td></tr><tr><td>第2次递归</td><td>栈：[(5,1)，(4,5)，(3,20)]</td><td>栈：[(3,20)]</td></tr><tr><td>第3次递归</td><td>栈：[(5,1)，(4,5)，(3,20)，(2,60)]</td><td>栈：[(2,60)]</td></tr><tr><td>第4次递归</td><td>栈：[(5,1)，(4,5)，(3,20)，(2,60)，(1,120)]</td><td>栈：[(1,120)]</td></tr><tr><td>终止返回</td><td>栈逐渐空，返回120</td><td>栈空，返回120</td></tr><tr><td>最大深度</td><td>5（随n增长）</td><td>1（固定）</td></tr></table>

# 尾递归用迭代法转化为非递归

- 尾递归通常可以直接转化为循环，循环部分实现递归的从小问题到大问题的集成，而从大问题到小问题的分解过程通常较为简单，已经体现在循环的设计中。

```txt
int fact(int n) { int  $f = 1$  for (int  $\mathrm{i} = 2$  ;  $\mathrm{i} <   = \mathrm{n}$  ;  $\mathrm{i + + })$  f=n\*f; return f; }
```

- 比尾递归算法复杂一些的是线性递归 (Linear Recursive) 算法, 线性递归算法有以下所示的一般形式:

```txt
proc(int n) {  
Stms1(n);  
if (Eval(n)) {  
proc(n-1); // 递归调用  
Stms2(n);  
}  
else  
Stms3(n);
```

• 其中，Stms1(n)、Eval(n)、Stms2(n)和Stms3(n)是可能与  $n$  有关的非递归计算的代码段，也可能为空。

![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/4720a16afb2355c55e94034a06f474077317db3150b52f33a0e27e46328a2932.jpg)


# - 该线性递归程序的执行流程:

核心原则：自上而下递归，自下而上回溯

执行流程总览（以初始调用  $\mathrm{proc}(\mathsf{n}_0)$  为例）

1. 触发初始层：调用 proc(n₀)，先执行 Stms1(n₀)

2. 判断递归条件：

。若  $\operatorname {Eval}(n_0) =$  False：直接执行Stms3(n0），程序结束（无递归）

。若  $\operatorname {Eval}(n_0) = \operatorname {True}$  ：进入递归，先执行子层proc(no-1)，暂停Stms2(no)

3. 子层递归循环：子层 proc(n0-1) 重复“Stms1→Eval 判断→递归 / 终止”流程，直到触发 Eval(k)=False

4. 回溯执行：从最底层终止节点开始，向上逐层执行暂停的 Stms2，最终回到 proc(n₀) 执行 Stms2(n₀)，程序结束

# - 该线性递归程序的执行流程（n=5, Eval(3)第一次为何）

<table><tr><td>步骤</td><td>递归栈状态（栈帧：n [待执行操作]）</td><td>说明</td></tr><tr><td>1</td><td>5 [Stms1后，待判断Eval(5)]</td><td>初始调用，执行 Stms1 (5)</td></tr><tr><td>2</td><td>5 [待Stms2(5)], 4 [Stms1后，待判断Eval(4)]</td><td>Eval (5)=true，调用 proc (4)</td></tr><tr><td>3</td><td>5 [待Stms2(5)], 4 [待Stms2(4)], 3 [Stms1后，待判断 Eval(3)]</td><td>Eval (4)=true，调用 proc (3)</td></tr><tr><td>4</td><td>5 [待Stms2(5)], 4 [待Stms2(4)]</td><td>Eval (3)=false，执行 Stms3 (3) 后弹出 proc (3)</td></tr><tr><td>5</td><td>5 [待Stms2(5)]</td><td>proc (4) 返回，执行 Stms2 (4) 后弹出</td></tr><tr><td>6</td><td>[]</td><td>proc (5) 返回，执行 Stms2 (5) 后弹出，栈空</td></tr></table>

- 线性递归能机械地被转换成一个功能等价的非递归描述：

$\bullet$  proc(int n) {

• int k=n+1;

• do {
1

$\mathrm{k} - - ;$

Stms1(k);

while Eval(k);

Stms3(k);

while  $(k < n)$  {

• k++;

Stms2(k);

$\bullet$

[\text{.}]

例如：对于求阶乘的问题，如果要考虑得更完善，需要防止在计算过程中发生溢出等异常情况。为此，按照线性递归的一般形式把求阶乘的程序改写为：

- void factorial(int n, int &temp) {

• if (n<0) { cerr<<n<<“不能为负数” <<endl; exit(1); }

if  $(n > 0)$  {

- factorial(n-1, temp);

if (Max/temp>n) { temp=temp*n; }

//Max 为允许的最大整数

• else { cerr<<“计算结果溢出” <<endl; exit(1); }

$\bullet$

else temp=1;

$\bullet$

- 这个程序可以套用线性递归转非递归的模式转换为：

- void factorial(int n, int &temp) {

• int k=n+1;

• do {

• k--;

• if (k<0) { cerr<<k<<“不能为负数” <<endl; exit(1); }

\} while  $(k > 0)$ ;

• temp=1;

while  $(k < n)$  {

$\mathrm{k} + + \mathrm{;}$

• if (Max/temp>k) temp=temp*k;

• else { cerr<<“计算结果溢出” <<endl; exit(1); }

[\text{.}]

$\bullet$

# 单向递归

- 单向递归可以有多个递归调用，但这些调用都必须处于递归调用过程的最后。

- 单向递归同时要求递归过程中的这些递归调用的参数由主调用过程的参数决定，而处在同一递归层次的递归调用之间参数不会互相影响。

- 尾递归可以看作一种特殊的单向递归（只有一个递归调用）。

# 计算斐波那契数列的函数  $\operatorname{Fib}(\mathfrak{n})$  的定

$$
\text {义} \operatorname {F i b} (n) = \left\{ \begin{array}{c c} n, & n = 0, 1 \\ \operatorname {F i b} (n - 1) + \operatorname {F i b} (n - 2), & n > 1 \end{array} \right.
$$

如  $\mathrm{F}_{0} = 0, \mathrm{~F}_{1} = 1, \mathrm{~F}_{2} = 1, \mathrm{~F}_{3} = 2, \mathrm{~F}_{4} = 3, \mathrm{~F}_{5} = 5$

# 求解斐波那契数列的递归算法

long Fib(long n) {

if  $(n <= 1)$  return n;

else return Fib(n-1) + Fib(n-2);

}

# 单向递归用迭代法转化为非递归

程序5-5 求斐波那契数的非递归算法

```lisp
long fib_NRcv(long n) { if((n==0)||(n==1)) return n; long f2=0, f1=1, current; for (int i=2;i<=n;i++) { current=f2+f1; f2=f1; f1=current; } return current; }
```

![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/c354b83819d6e79b5e24ca24ac38584d42c463a0df8ea75969b3f4e0f6c5e96a.jpg)



斐波那契数列的递归调用树


![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/b4ad89f670cf53956c194e969564da8f42d5cfe9c6881f10761d471ff270d0a8.jpg)


# 栈结点

n tag

tag=1，向左递归;

tag=2，向右递归;

struct Node {

long n;

int tag;

};

# 单向递归用栈转化为非递归


long Fibonacci(long n) {


```txt
Stack <Node> S; Node w; long sum=0; // 反复执行直到所有终端结点数据累加完  
do {  
    while (n>1) {  
        w.n=n; w.tag=1; S.Push(w);  
        n--;  
    } // 向左递归到底，边走边进栈  
sum=sum+n; // 执行求和
```

```javascript
while (S.IsEmpty() == false) {
    w = S.GetTop();
    S.Pop();
    if (w.tag == 1) { // 改为向右递归
        w.tag = 2; S.Push(w);
        n = w.n - 2; // F(n) 右側為 F(n-2)
        break;
    }
} while (!S.IsEmpty());
return sum;
```

# 5.3 广义表 (General Lists)

- 广义表的概念  $n(\geq 0)$  个表元素组成的有限序列, 记作:

$$
L S \left(a _ {0}, a _ {1}, a _ {2}, \dots , a _ {n - 1}\right)
$$

-  $LS$  是表名,  $a_{i}$  是表元素, 它可以是表 (称为子表), 可以是数据元素 (称为原子)。

-  $n$  为表的长度,  $n = 0$  的广义表为空表。

-  $n > 0$  时, 表的第一个表元素称为广义表的表头 (Head), 除此之外, 其它表元素组成的表称为广义表的表尾 (Tail)。Head(LS)= $a_{0}$ , Tail(LS)=( $a_{1}, a_{2}, \ldots, a_{n-1}$ )。

# 广义表的一些例子

$$
A = ()
$$

空表，长度为 0，无表头和表尾。

$$
B = (6, 2)
$$

线性表（只包括原子），长度为2，表头为6，表尾为(2)。

$$
C = \left( \begin{array}{l} ' a ^ {\prime}, (5, 3, ' x ^ {\prime}) \end{array} \right)
$$

长度为 2 , 表头为 ‘  $a$  ’, 表尾为  $((5,3,'x'))$  (子表)。

$$
D = (B, C, A)
$$

长度为 3 , 表头为 B , 表尾为  $(C, A)$  。

$$
\boldsymbol {E} = (\boldsymbol {B}, \boldsymbol {D})
$$

长度为 2 , 表头为 B , 表尾为 (D)。

$$
F = (4, F)
$$

长度为 2 , 表头为 4 , 表尾为  $(F)$  。

# 广义表的特性

# - 有次序性

- 在广义表中，各表元素在表中以线性序列排列，每个元素至多一个直接前驱，一个直接后继，且次序不能交换。

# - 有长度

- 广义表中元素个数一定，不能是无限的，可以是空表。

# - 有深度

- 广义表是多层次结构，表元素可以是原表的子表，子表的表元素还可以是子表等等。

# - 可递归

- 广义表可以是自己的子表（如  $F$ ）。

# - 可共享

- 广义表可以为其它广义表共享一共享表（如  $B$ ）。

# 广义表结点定义

(I)

tag

dlink/data

link

• 标志域 tag——结点类型，tag=0，原子；tag=1，子表。

- 信息域——tag=0时，存放数据值(data)；tag=1时，存放指向子表表头的指针(dlink)。

- 尾指针域 link——tag=0 时，指向该表第一个结点；tag≠0 时，指向同一层下一个结点。

![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/83dc598f7d7d514961e80fa8444829256776585b38497433fe50af3caf36605a.jpg)


![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/47e6c5469a139f6fe321ecc565241a81bc945e21e2cb32047664dad1702f63d5.jpg)


$\cdot \mathrm{A} = (\mathrm{a},(\mathrm{b},\mathrm{c}))$

$\cdot \mathrm{B} = (\mathrm{A}, \mathrm{A}, ()$

$\cdot \mathrm{C} = (\mathrm{a},(\mathrm{a},(\mathrm{a},\dots)))$  （递归表）

用链接结构表示广义表

# 广义表结点定义

(II)

tag

info

tlink

• 标志域 tag——结点类型，tag=0，表头；tag=1，原子；tag=2，子表。

- 信息域 info——tag=0 时，存放引用计数 (ref); tag=1 时，存放数据值 (value); tag=2 时，存放指向子表表头的指针 (hlink)。

- 尾指针域 tlink——tag=0 时，指向该表第一个结点；tag≠0 时，指向同一层下一个结点。

$$
A = ()
$$

空表，长度为0，无表头和表尾。

$$
B = (6, 2)
$$

线性表（只包括原子），长度为2，表头为6，表尾为(2)。

$$
C = \left(^ {\prime} a ^ {\prime}, (5, 3, ^ {\prime} x ^ {\prime})\right)
$$

长度为 2 , 表头为 ‘  $a$ , 表尾为  $((5, 3, 'x'))$  (子表)。

$$
\boldsymbol {D} = (\boldsymbol {B}, \boldsymbol {C}, \boldsymbol {A})
$$

长度为 3 , 表头为 B , 表尾为  $(C, A)$  。

$$
\boldsymbol {E} = (\boldsymbol {B}, \boldsymbol {D})
$$

长度为 2 , 表头为 B , 表尾为 (D)。

$$
F = (4, F)
$$

长度为 2 , 表头为 4 , 表尾为  $(F)$  。

![](file://~/MinerU/05 第五章.pptx-050663c4-2483-4226-9917-a8cb4f6a0fa8/images/e99c6156ccd580c8af051eddc361e209ec5b226b4ce411cf3c6d5e84e65062c9.jpg)


# 程序 5-1 广义表的类定义

define HEAD 0 // 表头结点类型

define INTEGER 1 // 整数类型

define CHAR 2 // 字符类型

define LIST 3 //子表类型

class GenList; // 广义表的前向引用声明

class GenListNode { // 广义表结点的类定义

friend class GenList;

private:

int tag; //=0/1/2/3，表示 HEAD/INTEGER/CHAR/list

GenListNode *link; // 指向同一层下一结点的指针

union { // 联合

int ref; //tag=HEAD，表头结点类型，用于存放引用计数

int integer_data; //tag=INTEGER，存放整数值

char char_data; //tag=CHAR，存放字符值

GenListNode *dlink; //tag=LIST，存放指向子表的指针

} data;


public:



GenListNode & Data(GenListNode *elem);



// 返回表元素 elem 的值



int NodeTypw( GenListNode *elem)



{ return elem->tag; }



// 返回表元素 elem 的元素值的数据类型



void SetData(GenListNode *elem, GenListNode &x);



// 将表元素 elem 中的值修改为 x


```txt
};
```

class GenList { // 广义表类定义

# private:

GenListNode *first; // 广义表头指针

# public:

GenList(); // 构造函数

~GenList(); // 析构函数

GenListNode & Head();

// 返回广义表第一个元素的值，即表头元素

GenListNode & Tail();

// 返回广义表除第一个元素外其它元素组成的表，即表尾

GenListNode * First(); // 返回广义表的第一个元素

GenListNode * Next(GenListNode *elem);

// 返回表元素 elem 的直接后继元素

void Push( Gen ListNode &x);

// 将一个包含值  $x$  的元素加入到广义表的最前面

GenList & Addon(GenList & list, GenListItem & x);

// 返回一个以 x 为头，list 为尾的新表

void SetHead(GenListNode &x);

// 将广义表的头元素重置为  $\mathbf{X}$

void SetNext(GenListNode *elem1, GenListNOde *elem2);

// 将 elem2 插到表中元素 elem1 后

void SetTail(GenList *list); // 将 list 定义为广义表的尾

GenListNode * Copy( GenListNode *ls); // 广义表的复制

int Equal(GenListNode *s, GenListNode *t);

// 判断两个广义表是否相等

int Depth(GenListNode *s, GenListNode *t);

// 计算一个非递归表的深度

void Remove( GenListNode *ls);

// 将以 ls 为表头结点的广义表结构释放

int CreateList( Gen ListNode *ls, char *s);

// 从广义表的字符串描述s出发

// 建立一个带表头结点的广义表结构

# 广义表的递归算法

# 广义表的复制算法

- 任何一个非空的广义表均可分为表头、表尾两个部分, 则一对确定的表头和表尾可唯一确定一个广义表。

- 复制一个广义表时，需要分别复制其表头和表尾，然后再合成，其前提是广义表不是共享表或递归表。

# 程序 5-2 广义表的复制

```cpp
GenListNode * GenList::Copy(GenListNode *ls)  
{ // 复制一个由 ls 指示的无共享子表的非递归表  
GenListNode *q=NULL;  
if (ls!=NULL)  
{  
    q=new GenListNode; // 创建一个新结点  
    q->tag=ls->tag; // 复制类型标记  
    switch (ls->tag)  
{ // 根据结点类型 tag 复制值域信息  
    case HEAD: // 表头结点  
        q->data.ref=ls->data.ref; break;  
    case INTEGER: // 整型原子结点  
        q->data(integer_data=ls->data(integer_data; break;  
        case CHAR: // 字符型原子结点  
            q->data.char_data=ls->data.char_data; break;  
        case LIST: // 子表结点  
            q->data.dlink=Copy(ls->data.dlink); break;  
    }  
    q->link=Copy(ls->link); // 复制同一层下一结点为头的表  
}  
return q;
```

# 判断两个广义表是否相等的算法

# - 判断两个广义表是否相等

- 不但两个广义表具有相同的结构，而且对应的数据成员具有相等的值。

如果两个广义表都是空表，则相等。

如果两个广义表的对应结点都是原子结点，对应项的值相等，再递归比较同一层后面的表元素。

- 如果两个广义表中对应项是子表结点, 则递归比较相应的子表。

# 程序 5-3 判别两个广义表是否相同

int GenList :: Equal(GenListNode *s, GenListNode *t)

// 判断由 s 与 t 指示的两个广义表是否相等

int x;

if (s->link == NULL && t->link == NULL) return 1;

// 表 s 与表 t 都是空表或者所有结点都比较完

if (s->link!=NULL && t->link!=NULL &&

$$
s - > l i n k - > t a g = = t - > l i n k - > t a g)
$$

// 两个广义表非空且结点类型标志相同

if (s->link->tag==INTEGER) // 整型原子结点

if (s->link->data(integer_data==

t->link->data(integer_data)

// 比较对应整数值

$\mathrm{x} = 1$ ;

else  $x = 0$ ;

```c
else if (s->link->tag==CHAR) //字符型原子结点  
if (s->link->data.char_data==t->link->data.char_data)  
//比较对应字符值  
x=1;  
else x=0;  
else x Equal(s->link->data.dlink, t->link->data.dlink);  
//子表结点，递归比较其子表  
if (x) return Equal(s->link, t->link);  
//当前结点相等，递归比较同一层的下一结点  
//若当前结点不等，则不再递归比较  
}  
return 0;
```

# 本章小结

# 知识点

一 递归算法和非递归算法

一广义表及其递归算法