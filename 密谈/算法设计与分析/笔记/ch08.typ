#import "util.typ": *

= 第八章 NP 与计算难解性

== 8.1 多项式时间归约（Polynomial-Time Reductions）

=== 归约的定义

*归约（reduction）* 是 NP 完全性理论的基石。直观而言，若问题 $X$ 可以"翻译"为问题 $Y$，且翻译过程由多项式时间算法完成，则称 $X$ 可*多项式时间归约*到 $Y$，记作 $X <=_p Y$。

*定义。* 问题 $X$ 多项式时间归约到问题 $Y$（$X <=_p Y$）如果存在一个多项式时间可计算的函数 $f$，使得对任意 $X$ 的实例 $x$，$f(x)$ 是 $Y$ 的实例，且
$x text("是 YES") <==> f(x) text("是 YES")$。

函数 $f$ 称为*归约函数（reduction function）*，该归约本身称为 *Karp 归约（Karp reduction）*。

*核心观察。* 若 $X <=_p Y$ 且 $Y$ 可在多项式时间内求解，则 $X$ 也可在多项式时间内求解——先计算 $f(x)$，再在 $Y$ 的算法上运行。

// 小图：归约方向
#box(
  stroke: 0.5pt + gray,
  inset: 8pt,
  width: 100%,
  [$X$ 的算法 $x$ $arrow.r.double$ $f$ $arrow.r.double$ $f(x)$ $arrow.r.double$ $Y$ 的算法 $arrow.r.double$ YES/NO],
)

*传递性（transitivity）。* 若 $X <=_p Y$ 且 $Y <=_p Z$，则 $X <=_p Z$。传递性使得归约链的构造成为可能。

=== 独立集与点覆盖的等价性

*独立集（Independent Set）*：给定图 $G = (V, E)$ 和整数 $k$，是否存在 $|S| >= k$ 且 $S$ 中任意两点无边相连？

*点覆盖（Vertex Cover）*：给定图 $G$ 和整数 $k$，是否存在 $|C| <= k$ 且每条边至少有一个端点在 $C$ 中？

*定理。* 在任意无向图中，$S$ 是独立集当且仅当 $V - S$ 是点覆盖。

*证明。* 若 $S$ 是独立集，则每条边 $(u, v)$ 不能同时落在 $S$ 中，因此至少一端在 $V - S$ 中，故 $V - S$ 是点覆盖。反之同理。

因此独立集与点覆盖的判定问题可通过互补关系直接相互归约。

*推论。* 独立集 $<=_p$ 点覆盖，且点覆盖 $<=_p$ 独立集（取 $k' = n - k$）。

=== 点覆盖归约到集合覆盖

*集合覆盖（Set Cover）*：给定宇宙 $U$、子集族 $cal(S) = {S_1, ..., S_m}$ 和整数 $k$，是否存在至多 $k$ 个子集使其并集等于 $U$？

*归约点覆盖 $<=_p$ 集合覆盖。* 对任意图 $G = (V, E)$ 和整数 $k$，构造集合覆盖实例：
- 宇宙 $U = E$（每条边对应一个元素）；
- 每个顶点 $v in V$ 对应一个子集 $S_v = {e in E : e text("与") v text("关联")}$；
- 阈值 $k$ 保持不变。

$G$ 有大小为 $k$ 的点覆盖当且仅当 $cal(S)$ 有 $k$ 个子集覆盖 $U$。归约可在 $O(m + n)$ 时间内完成。

== 8.2 通过"构件"归约：可满足性问题（Reductions via "Gadgets": The Satisfiability Problem）

=== SAT 与 3-SAT

*布尔可满足性问题（SAT）*：给定布尔公式 $phi$（由变量、$and$、$or$、$not$ 组成的合取范式 CNF），是否存在一组真值赋值使得 $phi = text("true")$？

*3-SAT*：CNF 中每个子句（clause）恰包含 3 个*文字（literal）*（变量或其否定）。

3-SAT 是 SAT 的特例，但 3-SAT 同样难：任意 SAT 问题可在多项式时间内转化为 3-SAT（通过引入新变量将长子句拆分为多个 3-子句）。

=== 3-SAT 归约到独立集

通过构造*构件（gadget）*实现归约。构造 $3 text("-SAT") <=_p text("独立集")$：

给定 $3$-$text("CNF")$ 公式 $phi$，含 $m$ 个子句 $C_1, ..., C_m$ 和 $n$ 个变量 $x_1, ..., x_n$。构造图 $G = (V, E)$ 如下：

1. *子句构件（clause gadget）*：对每个子句 $C_j = (ell_1 or ell_2 or ell_3)$，在 $G$ 中创建三个顶点，构成三角形（每对顶点间有边）。三角形内的三个顶点相互冲突——任何独立集最多从每个三角形中选取一个顶点。

2. *冲突边（conflict edges）*：对任意两个子句中的文字 $ell$ 和 $ell'$，若 $ell = not ell'$（即互为否定），则在对应顶点之间添加一条边。这确保独立集中不会同时选取一对互补文字。

*归约正确性。* 设 $m$ 为子句数，$k = m$。

（$=>$）若 $phi$ 可满足，任取每个子句中一个为真的文字 $ell_j$，对应顶点构成大小为 $m$ 的独立集（互不冲突）。

（$<=$）若 $G$ 有大小为 $m$ 的独立集 $S$，它必须从每个三角形中恰好选取一个顶点。将这些顶点对应的文字赋值为真：由于 $S$ 中没有冲突边，不存在 $ell$ 和 $not ell$ 同时被选，故赋值一致。每个子句至少有一个文字被选，故 $phi$ 被满足。

*构件（gadget）*的概念：在归约中，通过精心构造的小型子图或子结构来模拟另一个问题的约束条件。三角形的"每子句选一"约束和冲突边的"一致赋值"约束均是构件的典型应用。

== 8.3 高效认证与 NP 的定义（Efficient Certification and the Definition of NP）

=== 判定问题

*判定问题（decision problem）* 是答案为 YES 或 NO 的问题。例如：
- "图 $G$ 是否存在大小为 $k$ 的独立集？"
- "公式 $phi$ 是否可满足？"

优化问题通常可转化为判定问题。NP 完全性理论主要针对判定问题。

=== 认证者与证书

*认证者（certifier / verifier）* 是一个算法 $C(s, t)$，接受实例 $s$ 和*证书（certificate）* $t$ 作为输入，并输出 YES 或 NO。认证者要求：
- 当 $s$ 是 YES 实例时，存在某个证书 $t$ 使得 $C(s, t) = text("YES")$；
- 当 $s$ 是 NO 实例时，对所有证书 $t$ 有 $C(s, t) = text("NO")$；
- $C$ 的运行时间关于 $|s|$ 是*多项式*的（但证书大小也被多项式界住）。

*例子。*

- *合数（Composite）*：实例 $s$ 是一个正整数。证书 $t$ 是 $s$ 的一个非平凡因子。$C(s, t)$ 检查 $1 < t < s$ 且 $t mid s$。
- *3-SAT*：实例 $s$ 是一个 $3$-$text("CNF")$ 公式。证书 $t$ 是一个真值赋值。$C(s, t)$ 计算在 $t$ 下的公式值。
- *哈密顿回路（Hamiltonian Cycle）*：证书 $t$ 是一个顶点序列（排列）。$C$ 检查该序列是否构成简单回路且覆盖所有顶点。

=== NP 的形式化定义

*定义。* 判定问题 $X$ 属于 *NP（非确定性多项式时间，Nondeterministic Polynomial Time）* 如果存在多项式 $p$ 和多项式时间认证者 $C$，使得对所有实例 $s$：

$s in X  <=>  exists t, |t| <= p(|s|), C(s, t) = text("YES")$

其中 $t$ 是证书，$|t|$ 为 $t$ 的编码长度。

*P 与 NP 的关系。* $P subset.eq "NP"$：若问题在 $P$ 中，可直接给出一个平凡的认证者（忽略证书，直接求解）。是否 $P = "NP"$ 是千禧年难题之首。

*直觉。* NP 包含所有"解可被快速验证"的问题。非确定性体现在：若我们能"猜"出正确的证书 $t$，就可以在多项式时间内验证。

=== 其他相关类

- *EXP*：可在指数时间 $O(2^{n^c})$ 内求解的问题。$"NP" subset.eq "EXP"$（枚举所有可能的证书）。
- *co-NP*：补问题的判定在 NP 中的类。$P subset.eq "NP"$ 且 $P subset.eq text("co-NP")$。（本章跳过 co-NP 细节。）

== 8.4 NP 完全问题（NP-Complete Problems）

=== NP-hard 与 NP-complete

*定义。* 问题 $Y$ 是 *NP-hard（NP 难）* 如果对所有 $X in "NP"$，有 $X <=_p Y$。

*定义。* 问题 $Y$ 是 *NP-complete（NP 完全）* 如果 $Y in "NP"$ 且 $Y$ 是 NP-hard。

直观地说，NP-complete 问题是 NP 类中"最难"的问题——若其中任何一个可在多项式时间内求解，则整个 NP 类都可多项式求解。

=== Cook-Levin 定理

*电路可满足性问题（Circuit-SAT）*：给定一个布尔电路（由 $and$、$or$、$not$ 门构成，若干输入位，单输出），是否存在输入赋值使得输出为 1？

*Cook-Levin 定理（Cook 1971, Levin 1973）*：Circuit-SAT 是 NP-complete 的。

*证明思路（高屋建瓴）。* 对任意 $X in "NP"$，设 $C$ 为其多项式时间认证者，$C(s, t)$ 在某个计算模型（如 RAM 机或图灵机）上运行 $T = p(|s|)$ 步。可将 $C$ 在 $T$ 步内的计算过程编码为一个巨大的布尔电路 $Phi$，其中：
- 电路的输入对应证书 $t$ 的每一位；
- 电路的门模拟 $C$ 每一步的状态转移（寄存器、内存、控制逻辑等）；
- 输出为 1 当且仅当 $C$ 在 $T$ 步内输出 YES。

于是 $Phi$ 可满足当且仅当存在 $t$ 使得 $C(s, t) = text("YES")$，即 $s in X$。这就给出了 $X <=_p text("CIRCUIT-SAT")$。

// 小图：计算过程编码为电路
#box(
  stroke: 0.5pt + gray,
  inset: 8pt,
  width: 100%,
  [
  认证者 $C(s, t)$ 的状态（寄存器、内存、程序计数器）在每一步被编码为电路门。
  整个 $T$ 步的计算展开为一个"时间-空间"表格，每行对应一个时间步，
  门之间的连线表示因果依赖。
  ],
)

=== 归约链的建立

一旦有了第一个 NP-complete 问题，就无需每次都从所有 NP 问题出发。

*通用策略*：要证明问题 $Y$ 是 NP-hard，只需找已知 NP-complete 的 $X$ 并证明 $X <=_p Y$；若额外有 $Y in "NP"$，则 $Y$ 是 NP-complete。

*核心归约链：*

$text("CIRCUIT-SAT") <=_p text("SAT") <=_p 3 text("-SAT")$

CIRCUIT-SAT 到 SAT 的归约将电路的每个门编码为子句，引入中间变量表示门的输出，最终得到 CNF 公式。SAT 到 3-SAT 的归约通过引入辅助变量将长子句拆分为 3-子句。

后续章节在此链的基础上继续扩展：

$3$-$"SAT"$ 可归约到 $"独立集"$、$"有向哈密顿回路"$、$"3D-匹配"$、$"3-着色"$、$"子集和"$ 等问题；$"独立集"$ 又归约到 $"点覆盖"$ 再归约到 $"集合覆盖"$；$"有向哈密顿回路"$ 归约到 $"哈密顿回路"$ 再归约到 $"TSP"$。

=== 归约配方

设计 $X <=_p Y$ 归约时的通用框架：

1. *确定已知 NP-complete 问题 $X$*。通常首选 3-SAT（灵活，允许约束模拟）。
2. *构造映射*。将 $X$ 的任意实例转化为 $Y$ 的实例。核心是设计*构件*来模拟 $X$ 中的约束。
3. *证明多项式时间*。构造步骤数应为 $|x|$ 的多项式。
4. *证明双向正确性*：
    - *YES $"=>"$ YES*：若 $x$ 是 YES 实例，利用 $x$ 的解构造 $f(x)$ 的解。
    - *YES $"<="$ YES*（即 NO $"=>"$ NO）：若 $f(x)$ 是 YES 实例，利用 $f(x)$ 的解反推 $x$ 的解。

*常见陷阱。* 归约方向不可逆——我们约化 $X$ 到 $Y$，而不是相反。证书（认证者中的 $t$）和归约函数（$f$）是两个不同的概念，勿混淆。

== 8.5 序列问题（Sequencing Problems）

序列问题要求为元素安排一种顺序（排列），使之满足特定约束。

=== 有向哈密顿回路

*有向哈密顿回路（Directed Hamiltonian Cycle）*：给定有向图 $G = (V, E)$，是否存在一条回路恰好访问每个顶点一次？

*3-SAT $<=_p$ 有向哈密顿回路*（归约概要）：

构造一个有向图，其中每个变量 $x_i$ 对应一个"变量构件"——一条水平链上的 $2m$ 个顶点（$m$ 为子句数），边在两个方向上都存在。向左遍历链意味着 $x_i = text("false")$，向右遍历意味着 $x_i = text("true")$。

每个子句 $C_j$ 对应一个"子句顶点" $c_j$，将其接入变量链中适当位置：若 $x_i$ 在 $C_j$ 中正出现，则在向右遍历 $x_i$ 链的第 $j$ 个位置时允许"旁路"到 $c_j$；若 $not x_i$ 出现，则在向左遍历时允许旁路。

*正确性直觉。* 任何哈密顿回路必须遍历每条变量链的全程（方向决定赋值），并在此过程中访问所有子句顶点。子句顶点可被访问当且仅当该子句被相应方向的旁路"触发"——这等价于该子句被满足。因此公式可满足当且仅当图存在哈密顿回路。

=== 哈密顿回路与旅行商问题

*哈密顿回路（Hamiltonian Cycle, HC）*：无向图中是否存在哈密顿回路？

*有向 HC $<=_p$ 无向 HC*：对有向图 $G$ 的每个顶点 $v$，替换为"三顶点构件" $v_"in"$、$v_"mid"$、$v_"out"$，构成路径 $v_"in" - v_"mid" - v_"out"$。原图的有向边 $(u, v)$ 转化为无向边 ${u_"out", v_"in"}$。新图有无向哈密顿回路当且仅当原图有有向哈密顿回路。

*旅行商问题（TSP）判定版本*：给定完全图 $K_n$ 上的边权 $d_{i j}$ 和整数 $B$，是否存在权值 $<= B$ 的哈密顿回路？

*无向 HC $<=_p$ TSP*：对图 $G = (V, E)$，构造 $K_n$，设
$d_{i j} = cases(0 &"if" {i, j} in E, 1 &"if" {i, j} not in E)$，
令 $B = 0$。$G$ 有哈密顿回路当且仅当 TSP 实例有最优值 $0$。

*归约链总结：*
$3$-$"SAT" <=_p$ $"有向 HC" <=_p$ $"无向 HC" <=_p$ $"TSP"$

== 8.6 划分问题（Partitioning Problems）

划分问题要求将集合中的元素划分为满足特定条件的子集。

=== 三维匹配

*三维匹配（3-Dimensional Matching, 3DM）*：给定三个互不相交的集合 $X$、$Y$、$Z$，各含 $n$ 个元素，以及三元组集合 $T subset.eq X times Y times Z$，是否存在 $n$ 个互不相交的三元组恰好覆盖 $X union Y union Z$？

3DM 是经典二部匹配的自然推广，但难度骤增——从 $P$ 跃至 NP-complete。

*3-SAT $<=_p$ 3DM*（归约概要）：

构造含三种角色的三元组：
- *变量构件*：对每个变量 $x_i$，创建一组三元组构成一条"轮环"，匹配必须从环中选取一组"真"三元组或"假"三元组，对应 $x_i = text("true")$ 或 $x_i = text("false")$。
- *子句构件*：对每个子句 $C_j$，创建若干三元组连接到变量构件中代表该子句中文字的位置。
- *垃圾收集（garbage collection）*：创建额外的三元组使得任何未被匹配的元素可以被"清扫"匹配，确保所有元素最终都被覆盖。

归约的核心：每个三元组的选择对应一种字面意义的指派，$phi$ 可满足当且仅当存在完美三维匹配。

=== 其他划分问题

许多划分相关问题是 NP-complete 的，包括：
- *图 3-着色（Graph 3-Colorability）*（参见 8.7 节）
- *恰好覆盖（Exact Cover）*：给定宇宙 $U$ 和子集族 $cal(S)$，是否存在 $cal(S)$ 的子族恰好覆盖 $U$ 中每个元素一次？
- *划分（Partition）*：给定一列整数，是否能将其分为和相等的两组？

== 8.7 图着色（Graph Coloring）

=== k-着色问题

*k-着色（k-Coloring）*：给定无向图 $G$，是否可以用 $k$ 种颜色为顶点着色，使得任意相邻的顶点颜色不同？若 $k = 3$，称为 *3-着色（3-COLOR）*。

着色问题是约束满足的典型代表：每个顶点是一个变量，其域为 ${1, ..., k}$，每条边是一个约束 $c(u) != c(v)$。

对于 $k = 2$（二部性判定），存在线性时间算法。对于 $k >= 3$，问题变为 NP-complete。

=== 3-SAT 归约到 3-着色

*3-SAT $<=_p$ 3-COLOR*：

构造图 $G$，包含以下部分：

1. *调色板（palette）*：三个特殊顶点 $T$（真）、$F$（假）、$B$（基色），形成一个三角形，因此必须使用三种不同的颜色。将 $T$ 的颜色称为"真色"，$F$ 的颜色称为"假色"，$B$ 的颜色称为"基色"。

2. *变量构件*：对每个变量 $x$，创建顶点 $x$ 和 $not x$，添加边 $(x, not x)$、$(x, B)$、$(not x, B)$。由于 $B$ 固定为基色，$x$ 和 $not x$ 分别被限制为真色或假色（二者必须异色）。

3. *子句构件*：对每个子句 $C = (a or b or c)$（其中 $a, b, c$ 是文字顶点），构造一个"OR-门"子图——包含 6 个附加顶点，将 $a, b, c$ 与 $F$（假色顶点）连接，使得该子图可被 3-着色当且仅当 $a, b, c$ 不全为假色。

// 说明性小图：OR-gadget 结构（文字描述）
#box(
  stroke: 0.5pt + gray,
  inset: 8pt,
  width: 100%,
  [
  子句构件将三个文字顶点 $a, b, c$ 接入一个包含 5 个辅助节点的子图，
  该子图与 $F$ 相连。若 $a = b = c = text("false")$，子图无法 3-着色；
  否则可着色。
  ],
)

*正确性。*
- 若 $phi$ 可满足，将真文字的"真色"对齐 $T$ 的颜色，假文字的"假色"对齐 $F$ 的颜色，再对每个子句构件适当地着色，即得合法 3-着色。
- 若 $G$ 可 3-着色，每个子句构件中至少有一个文字被着真色（否则 $F$ 与三个假色文字间的约束导致矛盾），对应每个子句至少有一个真文字，故 $phi$ 可满足。

=== 平面图 3-着色

*平面图 3-着色（PLANAR 3-COLOR）*：即使限制图为平面图，3-着色问题仍是 NP-complete 的。该结果意味着着色问题的困难性本质上不依赖于图的非平面性。

== 8.8 数值问题（Numerical Problems）

数值问题涉及整数及其算术运算。许多此类问题的困难性来自于数字的"大小"——位数多时穷举不可行。

=== 子集和问题

*子集和（Subset-Sum）*：给定正整数 $a_1, ..., a_n$ 和目标值 $B$，是否存在子集 $S$ 使得 $sum_{i in S} a_i = B$？

=== 3-SAT 归约到子集和

*3-SAT $<=_p$ Subset-Sum*：

给定 $3$-$"SAT"$ 公式 $phi$，含 $n$ 个变量 $x_1, ..., x_n$ 和 $m$ 个子句 $C_1, ..., C_m$。构造 $2n + 2m$ 个正整数和一个目标值 $W$。使用 $n + m$ 位数，采用"无进位"编码。

*数字构造（每位独立，互不干扰）：*

- 对每个变量 $x_i$，创建两个 $(n + m)$ 位数 $v_i$ 和 $v_i'$：
  - $v_i$ 的第 $i$ 位（变量位）为 $1$，第 $n + j$ 位（子句位）为 $1$ 若 $x_i$ 在 $C_j$ 中正出现；
  - $v_i'$ 的第 $i$ 位为 $1$，第 $n + j$ 位为 $1$ 若 $not x_i$ 在 $C_j$ 中出现。

- 对每个子句 $C_j$，创建两个*松弛数（slack numbers）* $s_j$ 和 $s_j'$，仅在子句位 $n + j$ 上为 $1$，其余为 $0$。

*目标值 $W$：* 每个变量位为 $1$，每个子句位为 $3$。

*正确性。*
- 变量位限制：$W$ 的变量位为 $1$，故对每个 $i$，恰好从 ${v_i, v_i'}$ 中选择一个——对应 $x_i$ 的赋值。
- 子句位限制：$W$ 的子句位为 $3$。设选中变量在该位的贡献为 $k$（$0 <= k <= 3$，即该子句中真文字的个数），则需从松弛数中选择 $3 - k$ 个（可用 $0$、$1$ 或 $2$ 个）来凑足 $3$。若 $k = 0$（该子句无真文字），则即使两个松弛数都选也只能到 $2$，无法达到 $3$。因此每个子句必须至少有一个真文字。

*注意。* 此处采用十进制（或其他大于总和的进制）以避免进位干扰。若用二进制，低位进位会破坏高位编码的独立性。

=== 背包问题与强 NP 完全性

*背包问题（Knapsack）判定版本*：给定 $n$ 个物品，每个有重量 $w_i$ 和价值 $v_i$，以及容量 $W$ 和收益目标 $V$，是否存在子集使得 $sum w_i <= W$ 且 $sum v_i >= V$？

背包问题包含子集和作为特例（令 $w_i = a_i$，$v_i = a_i$，$W = B$，$V = B$），故背包问题是 NP-complete 的。

*弱 NP 完全 vs 强 NP 完全*：子集和与背包问题的困难性依赖于数字的位数（即数值"大"）。当数字以*一元（unary）*编码时（即用 $k$ 个 1 表示数 $k$），子集和可在多项式时间内用动态规划求解（$O(n B)$，此时 $B$ 为输入规模）。这类问题称为*弱 NP 完全（weakly NP-complete）*。

对于*强 NP 完全（strongly NP-complete）*问题，即使输入以一元编码，仍然 NP-complete（如 3-SAT、哈密顿回路）。强 NP 完全性反映了一个问题的困难性本质上不依赖于数值大小。

== 8.10 难问题分类概览（A Partial Taxonomy of Hard Problems）

=== 六大分类

本章所介绍的 NP-complete 问题可分为六大类型，每类共享相似的核心约束结构：

#table(
  columns: (auto, auto, auto),
  inset: 8pt,
  stroke: 0.5pt,
  [*类型*], [*代表问题*], [*核心约束*],
  [包装 Packing], [独立集、集合打包（Set Packing）], [选尽可能多的元素而不冲突],
  [覆盖 Covering], [点覆盖、集合覆盖], [选尽可能少的元素来覆盖全体],
  [约束满足 Constraint Sat.], [Circuit-SAT、SAT、3-SAT], [满足所有布尔约束],
  [序列 Sequencing], [有向/无向哈密顿回路、TSP], [访问所有元素一次形成序],
  [划分 Partitioning], [3D-匹配、3-着色], [将元素分配到互斥类别中],
  [数值 Numerical], [子集和、背包], [从集合中选数满足总和条件],
)

=== 值得注意的例外

以下问题至今*既未被证明属于 $P$，也未被证明是 NP-complete*，它们是复杂度理论中最引人注目的开放问题：

- *分解（Factoring）*：给定整数 $N$，求其非平凡因子。属于 $"NP" ∩ "co-NP"$（证书即因子）。Shor 算法可在量子计算机上多项式求解。
- *图同构（Graph Isomorphism）*：给定两个图 $G$ 和 $H$，是否存在双射 $f: V(G) -> V(H)$ 保持边关系？属于 NP，但已知在大多数情况下有多项式算法（Babai 2015 提出拟多项式算法）。
- *纳什均衡（Nash Equilibrium）*：给定多人博弈，是否存在混合策略纳什均衡？属于 PPAD 类（而非 NP-complete）。

这些例外提醒我们：NP-complete 并非"难问题"的全部——在 P 和 NP-complete 之间可能存在具有中间复杂度的自然问题。
