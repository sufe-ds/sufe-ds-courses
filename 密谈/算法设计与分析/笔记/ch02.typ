#import "util.typ": *

= 第二章 算法分析基础

== 2.1 计算可处理性（Computational Tractability）

=== 效率的直觉

算法分析的目的是预测算法在给定输入规模下的运行时间。核心直觉是：*运行时间*（running time）应随输入规模的增大而缓慢增长。一个"高效"的算法应当在输入规模合理增大时仍能在可接受时间内完成计算。

通常用输入规模的函数来描述运行时间，记为 $T(n)$，其中 $n$ 表示输入的大小。例如，对 $n$ 个数排序的问题，输入规模可取 $n$。

=== 多项式时间作为效率标准

*定义。* 如果一个算法的运行时间 $T(n)$ 是输入规模 $n$ 的多项式函数，即存在常数 $c > 0$ 和 $d > 0$，使得对所有 $n$ 有 $T(n) <= c n^d$，则称该算法是*多项式时间*（polynomial-time）可解的。

多项式时间作为效率的形式化标准有以下原因：

- 多项式函数增长速度较慢，且对输入规模的适度增长不敏感；
- 多项式类在组合运算下封闭（加法、乘法、复合仍为多项式）；
- 忽略常数因子和低阶项后，多项式界刻画了算法的本质伸缩性；
- 指数函数增长极快，即使算法在小规模上看似可行，在 $n$ 稍大时也往往不可用。

=== 最坏情况分析（Worst-Case Analysis）

*定义。* 对于给定规模 $n$ 的所有可能输入，取运行时间的最大值作为该规模下的时间开销：

$ T(n) = limits(op("max"))_("输入 I, |I| = n") "算法在 I 上的运行时间" $

最坏情况分析是算法分析中最常用的准则，因为它提供了一个*保证*（guarantee）：算法在任何输入上的运行时间都不会超过 $T(n)$。这种保守性避免了依赖特定输入分布的风险。

=== 计算模型概述

为了在抽象层面比较算法，需要定义基本操作的单位成本。

- *图灵机模型*（Turing machine）：最底层的抽象，每一步读写一个符号，常作为可计算性的基础模型。
- *RAM 模型*（Random Access Machine）：假设每条简单指令（算术、比较、赋值、内存访问）耗时 $O(1)$。这是算法分析中最常用的模型。
- *位运算 / 对数代价模型*（bit model / logarithmic cost）：当操作数可能非常大时（如大整数运算），需考虑操作数位数的对数代价。

在本书中，除非特别说明，均采用 RAM 模型。

=== 其他分析类型简述

- *平摊分析*（amortized analysis）：考虑一个操作序列的*平均*开销，允许个别操作代价高，但保证序列的总开销被摊低。典型例子是动态数组的插入。
- *概率分析*（average-case / probabilistic analysis）：对输入分布做假设，分析算法在"平均"输入上的期望运行时间。最坏情况分析更强健，但当已知输入分布良好时，概率分析能给出更紧的界。

== 2.2 渐近增长率（Asymptotic Order of Growth）

=== 渐近记号

渐近分析的目的是忽略常数因子和低阶项，刻画算法在输入规模趋向无穷时运行时间的增长趋势。设 $T(n)$ 和 $f(n)$ 是定义在自然数上的正函数。

==== $O$ 记号——渐近上界

*定义。* $T(n) = O(f(n))$ 如果存在常数 $c > 0$ 和 $n_0 > 0$，使得对所有 $n >= n_0$，有

$ T(n) <= c · f(n) $

$O$ 表示"增长不超过 $f(n)$"，是最常用的上界记号。

==== $Ω$ 记号——渐近下界

*定义。* $T(n) = Ω(f(n))$ 如果存在常数 $c > 0$ 和 $n_0 > 0$，使得对所有 $n >= n_0$，有

$ T(n) >= c · f(n) $

$Ω$ 表示"增长至少与 $f(n)$ 一样快"，用于给出算法复杂度的下限。

==== $Θ$ 记号——渐近紧界

*定义。* $T(n) = Θ(f(n))$ 如果 $T(n) = O(f(n))$ 且 $T(n) = Ω(f(n))$ 同时成立。等价地，存在常数 $c_1, c_2 > 0$ 和 $n_0 > 0$，使得对 $n >= n_0$，

$ c_1 · f(n) <= T(n) <= c_2 · f(n) $

$Θ$ 表示"精确地以 $f(n)$ 的倍数增长"，给出算法复杂度的紧确刻画。

=== 渐近分析的重要性质

1. *常数因子无关。* 对任意常数 $k > 0$，$k · f(n) = Θ(f(n))$。
2. *低阶项无关。* 若 $T(n) = a_d n^d + a_(d-1) n^(d-1) + ... + a_0$，则 $T(n) = Θ(n^d)$。
3. *传递性。* 若 $f = O(g)$ 且 $g = O(h)$，则 $f = O(h)$（对 $Ω$、$Θ$ 同理）。
4. *对称性。* $f = Θ(g)$ 当且仅当 $g = Θ(f)$。

=== 常见函数层次

渐近增长率从低到高的典型序列：

$ O(1) < O(op("log") n) < O(n) < O(n op("log") n) < O(n^2) < O(n^3) < O(2^n) < O(n!) $

其中 $O(op("log") n)$ 是任意对数底（换底仅差常数因子）。

=== 极限判别法

若极限存在，可用以下准则比较两个函数的增长率：

$ op("lim")_(n -> oo) frac(f(n), g(n)) = cases(
  0 => O,
  c > 0 => Θ,
  oo => Ω
) $

其中第二行 $c > 0$ 表示有限非零常数；第三行 $Ω$ 后附注 $f ≠ Θ(g)$。

例如，$op("lim")_(n -> oo) frac(op("log") n, n) = 0$，故 $op("log") n = O(n)$ 且 $op("log") n ≠ Θ(n)$。

=== 多变量渐近界

当算法的时间复杂度同时依赖于多个参数时（如一个图有 $n$ 个顶点和 $m$ 条边），需使用多变量渐近记号。例如，$T(n,m) = O(n + m)$ 表示存在常数 $c, n_0, m_0$，使得对所有 $n >= n_0$ 或 $m >= m_0$ 有 $T(n,m) <= c (n + m)$。

多变量定义需注意：存在两个独立参数意味着边界条件可能更复杂，但核心思想一致——忽略常数因子，关注最主导的增长项。

== 2.3 用链表和数组实现稳定匹配算法（Implementing the Stable Matching Algorithm）

=== Gale-Shapley 算法回顾与 $O(n^2)$ 目标

第 1 章给出的 Gale-Shapley 算法描述虽简洁，但若直接实现，某些操作可能效率不足。本节给出一种使用基本数据结构的 $O(n^2)$ 实现，其中 $n$ 表示每组的个体数。

算法核心是每次选一位未匹配的男性 $m$，向其偏好列表中尚未提议过的最高排名女性 $w$ 提议。若 $w$ 空闲则暂定匹配；否则 $w$ 比较 $m$ 和当前伴侣 $m'$，择优保留。

=== 数据结构设计

为了实现每条操作 $O(1)$ 时间，需要以下数据结构：

1. *男性偏好列表*（preference list）。用一个 $n × n$ 的二维数组 `ManPref[m][i]` 表示男性 $m$ 偏好列表中排名第 $i$ 位的女性编号。
2. *女性偏好列表*（用逆偏好表实现）。用一个 $n × n$ 的二维数组 `WomanPref[w][i]` 表示女性 $w$ 偏好列表中排名第 $i$ 位的男性编号。但为了快速比较两个男性在 $w$ 心中的优先级，还需构造逆偏好表。
3. *逆偏好列表*（inverse preference list）。对每位女性 $w$，构造数组 `Ranking[w][m]`，其值为男性 $m$ 在 $w$ 偏好列表中的排名（从 0 或 1 开始编号）。这样判断 $w$ 是否更偏好 $m$ 而非 $m'$ 只需比较 `Ranking[w][m] < Ranking[w][m']`，$O(1)$ 时间。
4. *下一提议对象指针*（next proposal pointer）。数组 `Next[m]` 记录男性 $m$ 下一轮应提议的女性在其偏好列表中的当前位置，初始均为 1。
5. *当前匹配状态*。数组 `CurrentMatch[w]` 记录女性 $w$ 当前的伴侣（若空闲则为 0 或 `nil`），数组 `CurrentPartner[m]` 记录男性 $m$ 当前的伴侣。初始均为空。
6. *空闲男性列表*。可用栈或队列（由链表实现），存储所有尚未匹配的男性。

=== 算法伪代码

```python
def gale_shapley_efficient(man_pref, woman_pref, n):
    """Gale-Shapley 算法的 O(n²) 高效实现"""
    # 初始化
    current_match = [None] * (n + 1)
    next_proposal = [1] * (n + 1)
    free_men = list(range(1, n + 1))

    # 构建逆偏好表
    ranking = [[0] * (n + 1) for _ in range(n + 1)]
    for w in range(1, n + 1):
        for r in range(1, n + 1):
            man = woman_pref[w][r]
            ranking[w][man] = r

    # 主循环
    while free_men:
        m = free_men.pop(0)
        w = man_pref[m][next_proposal[m]]
        next_proposal[m] += 1

        if current_match[w] is None:
            current_match[w] = m
        else:
            m_prime = current_match[w]
            if ranking[w][m] < ranking[w][m_prime]:
                current_match[w] = m
                free_men.append(m_prime)

    return current_match
```

=== 复杂度分析

- *初始化阶段*：构建逆偏好表需要 $O(n^2)$ 时间。
- *循环次数*：每个男性最多向所有 $n$ 位女性各提议一次，故 `while` 循环最多执行 $n^2$ 次。
- *单次迭代*：每条操作（取队首、数组访问、比较排名、更新匹配）均为 $O(1)$。
- *总复杂度*：$O(n^2)$。

空间复杂度：$O(n^2)$（存储偏好表和逆偏好表）。

== 2.4 常见运行时间概览（A Survey of Common Running Times）

=== $O(1)$ —— 常数时间

*定义。* 运行时间与输入规模无关。无论 $n$ 多大，算法的操作次数是固定的。

*典型例子：* 数组的随机访问、在链表头部插入、交换两个变量值。

=== $O(op("log") n)$ —— 对数时间

*定义。* 运行时间与输入规模的对数成正比。底数不影响渐近阶。

*直觉：* 每次操作将问题规模缩小一个常数因子。

*典型例子：* 二分查找（binary search）——在有序数组中查找一个元素，每次将搜索区间缩小一半。也可以视为将 $n$ 反复除以 2 直到 1 所需的步骤数，即 $op("log")_2 n$。

=== $O(n)$ —— 线性时间

*定义。* 运行时间与输入规模成正比。

*直觉：* 需要对每个输入元素执行常数次操作。

*典型例子：* 扫描数组求最大值、线性搜索、合并两个有序列表。

=== $O(n op("log") n)$ —— 线性对数时间

*定义。* 运行时间为 $n$ 与 $op("log") n$ 的乘积。

*直觉：* 通常来自分治（divide-and-conquer）算法，即把问题分成两个规模大致相等的子问题，合并需要 $O(n)$ 时间。

*典型例子：* 归并排序（mergesort）、堆排序（heapsort）、快速排序的平均情况。这是比较排序（comparison-based sorting）的渐近下界。

=== $O(n^2)$ —— 平方时间

*定义。* 运行时间与 $n^2$ 成正比。

*直觉：* 典型来自两重嵌套循环，每层迭代 $O(n)$ 次。

*典型例子：* 选择排序、插入排序、朴素最近点对算法。

=== $O(n^3)$ —— 立方时间

*定义。* 运行时间与 $n^3$ 成正比。

*直觉：* 三重嵌套循环。常见于某些动态规划或需要枚举三元组的算法。

*典型例子：* Floyd-Warshall 全源最短路算法（$n$ 为顶点数）、朴素矩阵乘法。

=== $O(2^n)$ 与 $O(n!)$ —— 指数与阶乘时间

*定义。* 运行时间随 $n$ 指数增长（$O(2^n)$）或按阶乘增长（$O(n!)$）。

*直觉：* 常见于需要枚举所有子集（$2^n$）或所有排列（$n!$）的算法。

*典型例子：*
- $O(2^n)$：子集和问题的暴力搜索、旅行商问题（TSP）的枚举所有子集法。
- $O(n!)$：TSP 枚举所有排列、为 $n$ 个元素分派 $n$ 个任务的所有方案。

*重要观察：* 指数时间算法在 $n$ 稍大时即不可用（$2^(30) approx 10^9$，$30! approx 2.65 × 10^(32)$），因此多项式时间与指数时间之间的区别是算法设计中最基本的分界线。

=== 运行时间对比一览

#table(
  columns: 5,
  align: (left, right, right, right, right),
  table.header([增长阶], [$n=10$], [$n=100$], [$n=10^3$], [$n=10^6$]),
  [$log n$], [$3.3$], [$6.6$], [$10$], [$20$],
  [$n$], [$10$], [$10^2$], [$10^3$], [$10^6$],
  [$n log n$], [$33$], [$660$], [$10^4$], [$2 × 10^7$],
  [$n^2$], [$10^2$], [$10^4$], [$10^6$], [$10^(12)$],
  [$n^3$], [$10^3$], [$10^6$], [$10^9$], [$10^(18)$],
  [$2^n$], [$10^3$], [$1.27 × 10^(30)$], [$approx 10^(301)$], [—],
  [$n!$], [$3.6 × 10^6$], [$approx 10^(158)$], [—], [—],
)

（表中数值为近似数量级，忽略常数因子。）
