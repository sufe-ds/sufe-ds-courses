// 第七章：网络流
// 基于 Kleinberg & Tardos, Algorithm Design (2006)

#import "util.typ": *

// --- 定理环境 ---
#show heading.where(level: 1): it => {
  thm-counter.update(0)
  it
}

// ============================================================
// 第七章
// ============================================================
= 第七章 网络流

*网络流（network flow）* 是算法设计中最重要的工具之一。它不仅能够建模运输、通信等自然涉及流量的问题，还可将许多表面上与流无关的组合问题（如二部图匹配、项目选择）转化为流模型求解。本章的核心结果是*最大流最小割定理*（Max-Flow Min-Cut Theorem），以及 Ford-Fulkerson 算法及其改进。

== 7.1 最大流问题与 Ford-Fulkerson 算法（The Maximum-Flow Problem and the Ford-Fulkerson Algorithm）

=== 流网络的定义

一个*流网络（flow network）*是一个有向图 $G = (V, E)$，每条边 $e in E$ 有一个非负*容量（capacity）* $c_e >= 0$。图中指定两个特殊节点：*源点（source）* $s$ 和*汇点（sink）* $t$。约定每条边都有反向平行边的情况不存在（可等价避免）。

#definition[
  一个 *s-t 流（flow）* $f$ 是一个将每条边 $e$ 映射为非负实数 $f_e$ 的函数，满足：

  - *容量约束（capacity constraints）*：对每条边 $e in E$，$0 <= f_e <= c_e$。
  - *流量守恒（flow conservation）*：对每个节点 $v not in {s, t}$，
    $ sum_("out" "边" e "离开" v) f_e = sum_("in" "边" e "进入" v) f_e $
]

流 $f$ 的*值（value）*记为 $|f|$，定义为从 $s$ 流出的总流量：

$ |f| = sum_(e "离开" s) f_e - sum_(e "进入" s) f_e $

由于守恒约束，$|f|$ 也等于进入 $t$ 的净流量。

=== 残留图（Residual Graph）

给定流 $f$，可以构造*残留图（residual graph）* $G_f$，它包含与 $G$ 相同的节点集，但对每条参与流的边提供"撤销"的可能性。

#definition[
  残留图 $G_f = (V, E_f)$ 的边定义如下：
  - *正向边（forward edge）*：若 $f_e < c_e$，则在 $G_f$ 中加入边 $e$，容量为 $c_e - f_e$（即剩余可用容量）。
  - *反向边（backward edge）*：若 $f_e > 0$，则在 $G_f$ 中加入 $e$ 的反向边 $e'$，容量为 $f_e$（即已使用的流量，允许"退回"）。
]

$G_f$ 中的边容量称为*残留容量（residual capacity）*。其直观含义是：在 $G_f$ 中找到一条 $s$ 到 $t$ 的路径，意味着可以在原网络上进一步推送流量或撤销已有流量，从而增加总流值。

=== Ford-Fulkerson 算法

Ford-Fulkerson 算法的核心思想是：只要 $G_f$ 中存在 $s$ 到 $t$ 的路径（称为*增广路径（augmenting path）*），就沿该路径推送尽可能多的流量。

#definition[
  $G_f$ 中的一条 $s$-$t$ 路径 $P$，$P$ 上所有边的最小残留容量称为该路径的*瓶颈容量（bottleneck capacity）*：
  $"bottleneck"(G_f, P) = min_((u,v) in P) c_f(u,v)$
]

*算法：Ford-Fulkerson*

```python
def ford_fulkerson(G, s, t):
    """
    Ford-Fulkerson 最大流算法
    G: 流网络 (V, E)，每条边 e 有容量 c_e
    s: 源点
    t: 汇点
    返回: 最大流 f（每条边上的流量）
    """
    # 初始化：所有边流量为 0
    f = {e: 0 for e in G.edges}

    # 构造初始残留图 G_f（正向边容量 = 原始容量，反向边容量 = 0）
    G_f = build_residual_graph(G, f)

    # 反复增广：在残留图中找 s-t 路径
    while True:
        # 在 G_f 中找一条从 s 到 t 的路径（可用 BFS/DFS）
        P = find_path(G_f, s, t)
        if P is None:
            break  # 无增广路径，算法终止

        # 计算路径 P 的瓶颈容量（路径上各边残留容量的最小值）
        b = min(G_f[u][v] for (u, v) in P)

        # 沿路径 P 增广 b 单位流量
        for (u, v) in P:
            if is_original_edge(G, u, v):
                # 正向边：增加流量
                f[(u, v)] += b
            else:
                # 反向边：对应原图边 (v, u)，减少流量
                f[(v, u)] -= b

        # 更新残留图
        G_f = build_residual_graph(G, f)

    return f
```

=== 终止条件

当 $G_f$ 中不存在 $s$-$t$ 路径时，算法终止。此时 $s$ 和 $t$ 在 $G_f$ 中处于不同的连通分量，这正是最大流最小割定理的出发点。

*复杂性分析。* 若所有容量为整数，每次增广至少增加 1 单位流量，故增广次数不超过最大流值 $v^*$。每次增广需 $O(m)$ 时间（用 BFS 或 DFS 找路径），总时间 $O(m v^*)$。然而 $v^*$ 可能很大，且若容量为无理数，算法甚至可能无限循环。

== 7.2 最大流最小割定理（Maximum Flows and Minimum Cuts in a Network）

=== 割的定义

#definition[
  一个 *s-t 割（cut）* $(A, B)$ 是节点集 $V$ 的一个划分，满足 $A union B = V$、$A inter B = nothing$、$s in A$ 且 $t in B$。
]

#definition[
  割 $(A, B)$ 的*容量（capacity）*定义为从 $A$ 指向 $B$ 的所有边的容量之和：
  $c(A, B) = sum_{u in A, v in B, (u,v) in E} c_(u v)$
]

注意：从 $B$ 指向 $A$ 的边不计入割容量。

=== 流值引理

#lemma(name: "流值引理")[
  对任意 s-t 流 $f$ 和任意 s-t 割 $(A, B)$，有：
  $ |f| = f^"out"(A) - f^"in"(A) <= c(A, B) $
  其中 $f^"out"(A) = sum_{u in A, v in B} f_(u v)$，$f^"in"(A) = sum_{u in B, v in A} f_(u v)$。
]

#proof[
  由守恒约束，对任意 $v in A \\ {s}$，流入 $=$ 流出。将 $A$ 中所有节点的守恒等式相加，左侧净流出即为 $|f|$，右侧恰好为 $f^"out"(A) - f^"in"(A)$。由容量约束，$f^"out"(A) <= c(A, B)$ 且 $f^"in"(A) >= 0$，故 $|f| <= c(A, B)$。
]

由此可得*弱对偶性（weak duality）*：任意流的值不超过任意割的容量，从而最大流的值不超过最小割的容量。

#corollary[
  $max_{f} |f| <= min_{(A,B)} c(A, B)$
]

=== 最大流最小割定理

#theorem(name: "最大流最小割定理")[
  在任意流网络中，最大流的值等于最小割的容量。即：
  $max_{f} |f| = min_{(A,B)} c(A, B)$
]

#proof[
  设 $f$ 是 Ford-Fulkerson 算法终止时得到的流。此时 $G_f$ 中不存在从 $s$ 到 $t$ 的路径。令 $A$ 为 $G_f$ 中从 $s$ 可达的所有节点，$B = V \\ A$。显然 $s in A$、$t in B$，故 $(A, B)$ 是一个 s-t 割。

  对任意从 $A$ 指向 $B$ 的原图边 $e = (u, v)$，必有 $f_e = c_e$（否则 $G_f$ 中会有正向边 $(u, v)$ 使 $v$ 从 $s$ 可达）。对任意从 $B$ 指向 $A$ 的原图边 $e = (v, u)$，必有 $f_e = 0$（否则 $G_f$ 中会有反向边 $(u, v)$ 使 $v$ 从 $s$ 可达）。因此：
  $|f| = f^"out"(A) - f^"in"(A) = c(A, B) - 0 = c(A, B)$
  由弱对偶性，$f$ 即是最大流，$(A, B)$ 即是最小割。
]

=== 算法意义

该定理给出了最优性条件：Ford-Fulkerson 算法终止时得到的流就是最大流，且同时给出了一个最小割作为*证书（certificate）*。

#corollary[
  若所有容量均为整数，则存在一个取整数值的最大流。
]

== 7.3 选择好的增广路径（Choosing Good Augmenting Paths）

=== Ford-Fulkerson 的缺陷

Ford-Fulkerson 算法的复杂度 $O(m |f^*|)$ 依赖于最大流值 $|f^*|$。若每次增广仅增加很少流量（例如在坏情况下每次瓶颈容量为 1），则增广次数可能极大。更糟的是，若容量为无理数，算法可能无限循环而不收敛。

改进策略：选择"好"的增广路径，确保增广次数为多项式。

=== 容量缩放算法（Capacity Scaling）

容量缩放算法优先选择瓶颈容量大的路径，将增广次数控制在多项式级别。

==== 算法思想

引入缩放参数 $Delta$，初始化为不超过最大边容量的最大 2 的幂。在每一轮中，仅考虑残留容量 $>= Delta$ 的边构成的子图 $G_f(Delta)$，在此子图中找 $s$-$t$ 路径并增广。当 $G_f(Delta)$ 中无 $s$-$t$ 路径时，$Delta <- Delta / 2$，继续下一轮，直到 $Delta < 1$。

*算法：Scaling Max-Flow*

```python
def scaling_max_flow(G, s, t):
    """
    容量缩放最大流算法
    G: 流网络 (V, E)，每条边 e 有容量 c_e
    s: 源点
    t: 汇点
    返回: 最大流 f（每条边上的流量）
    """
    # 初始化：所有边流量为 0
    f = {e: 0 for e in G.edges}

    # 找最大容量 C，取不超过它的最大 2 的幂作为初始 Δ
    C = max(c_e for e in G.edges)
    Delta = 1 << (C.bit_length() - 1)  # 即 2^{⌊log₂ C⌋}

    while Delta >= 1:
        # 缩放阶段：在 G_f(Δ) 中反复增广
        # G_f(Δ) 是残留图中仅保留残留容量 >= Δ 的边的子图
        while True:
            G_f = build_scaled_residual_graph(G, f, Delta)
            P = find_path(G_f, s, t)
            if P is None:
                break  # 当前 Δ 阶段结束

            # 瓶颈容量 b >= Δ（由 G_f(Δ) 的定义保证）
            b = min(G_f[u][v] for (u, v) in P)

            # 沿 P 增广 b 单位流量
            for (u, v) in P:
                if is_original_edge(G, u, v):
                    f[(u, v)] += b
                else:
                    f[(v, u)] -= b

        # Δ 减半，进入下一缩放阶段
        Delta //= 2

    return f
```

其中 $G_f(Delta)$ 是 $G_f$ 的子图，仅保留残留容量 $>= Delta$ 的边。

==== 正确性

#lemma(name: "缩放正确性")[
  缩放算法在 $Delta = 1$ 时相当于在 $G_f$ 中反复增广，因此最终得到的流是最大流。
]

#proof[
  当 $Delta = 1$ 时，$G_f(1)$ 包含所有残留容量 $>= 1$ 的边。若 $G_f(1)$ 中无 $s$-$t$ 路径，则 $G_f$ 中也不存在 $s$-$t$ 路径（因为残留容量 0 的边不可用），因此流已达最大。
]

==== 复杂度分析

#theorem(name: "缩放算法复杂度")[
  容量缩放算法的复杂度为 $O(m^2 log C)$，其中 $C = max_e c_e$。
]

#proof[
  分为两部分：缩放阶段的数量和每个阶段内的增广次数。

  *缩放阶段数。* $Delta$ 从 $C$ 开始每次减半，直到 $< 1$，共 $|~ log_2 C ~| + 1 = O(log C)$ 个阶段。

  *每个阶段的增广次数。* 在每个 $Delta$ 阶段开始时，最大流值不超过当前流值加上 $m Delta$（因为每条边至多还能容纳 $Delta$ 的额外流量，引理细节略）。每次增广至少增加 $Delta$ 的流量，因此每个阶段内的增广次数不超过 $O(m)$。

  每次增广在 $G_f(Delta)$ 中找路径需 $O(m)$ 时间（BFS/DFS），故每个阶段 $O(m^2)$ 时间。乘以阶段数得 $O(m^2 log C)$。
]

=== 最短增广路径（Shortest Augmenting Path）

另一种策略：每次选择边数最少的增广路径（即 $G_f$ 中的最短 $s$-$t$ 路径，用 BFS 找到）。可以证明，若每次选最短路径，则增广次数为 $O(m n)$，总复杂度 $O(m^2 n)$。其核心观察是每次增广不会使任何节点到 $s$ 的距离减小，且每条边最多成为"瓶颈边" $O(n)$ 次。

== 7.5 第一应用：二部图匹配（A First Application: The Bipartite Matching Problem）

=== 问题定义

给定一个*二部图（bipartite graph）* $G = (X union Y, E)$，其中 $X$ 和 $Y$ 是顶点集，$E ⊆ X times Y$。一个*匹配（matching）* $M ⊆ E$ 是边的一个子集，其中任意两条边没有公共顶点。*最大匹配问题*（maximum matching problem）是求 $|M|$ 最大的匹配。

=== 最大流建模

将二部图匹配转化为最大流问题：

1. 添加源点 $s$ 和汇点 $t$。
2. 对每个 $x in X$，添加边 $(s, x)$，容量为 1。
3. 对每个 $y in Y$，添加边 $(y, t)$，容量为 1。
4. 对每条原图边 $(x, y) in E$，添加边 $(x, y)$，容量为 1。
5. 所有边的容量均为 1。

#figure(
  block(text(size: 0.9em)[
    建模后的网络图结构：$s -> X$（容量 1），$X -> Y$（容量 1），$Y -> t$（容量 1）。
  ]),
  caption: [二部图匹配的最大流建模],
)

=== 整数流定理

#theorem(name: "整数流定理 / Integrality Theorem")[
  若流网络中所有边的容量均为整数，则存在一个取整数值的最大流。进一步地，Ford-Fulkerson 算法及其改进算法（使用整数瓶颈容量）返回的流必为整数流。
]

#proof[
  整数容量下，增广路径的瓶颈容量始终为整数，因此每条边上的流量始终保持为整数。
]

对上述容量为 1 的网络应用整数流定理，最大流 $f^*$ 的每条边上流量为 0 或 1。$f_e = 1$ 的边 $(x, y)$ 构成一个匹配，且 $|f^*|$ 恰为匹配的大小。因此最大流对应最大匹配。

=== Hall 定理

最大流最小割定理的一个重要推论是*Hall 定理（Hall's Theorem / Marriage Theorem）*。

#corollary(name: "Hall 定理")[
  设二部图 $G = (X union Y, E)$，则 $G$ 存在一个完全匹配（$|X|$ 条边，覆盖所有 $X$ 顶点）当且仅当对每个子集 $S ⊆ X$，其邻居集 $N(S) ⊆ Y$ 满足 $|N(S)| >= |S|$。
]

#proof[
  考虑上述流网络。若不存在完全匹配，则最大流值 $< |X|$，即最小割容量 $< |X|$。分析最小割的结构即可得到违反 Hall 条件的子集 $S$。
]

== 7.6 有向图和无向图中的不相交路径（Disjoint Paths in Directed and Undirected Graphs）

=== 边不相交路径问题

给定有向图 $G = (V, E)$ 和两个指定节点 $s, t$，求最多数量的*边不相交（edge-disjoint）*的 $s$-$t$ 路径（即任意两条路径不能有公共边）。

=== 有向图的建模

将每条边的容量设为 1，求最大流。整数最大流中，每条被使用的边（$f_e = 1$）恰好属于一条 $s$-$t$ 路径，且由于边不相交约束，不同路径的边集不重叠。

#theorem(name: "Menger 定理（边版本）")[
  有向图中最大边不相交 $s$-$t$ 路径数等于最小 $s$-$t$ 割的容量。
]

#proof[
  在单位容量网络中，最大流值即为最大边不相交路径数；最小割容量即为最少需要删除的边数（每条割边的容量为 1，割容量即删除后可分离 $s$ 和 $t$ 的最小边数）。由最大流最小割定理，二者相等。
]

=== 无向图的处理

将无向图 $G$ 中每条无向边 $(u, v)$ 替换为两条方向相反的有向边 $(u, v)$ 和 $(v, u)$，每条容量设为 1。注意：两条路径不能使用同一无向边的不同方向——幸运的是，若某单位容量网络的最大流使用了两个方向的边，这相当于在无向图中重复使用同一条边，可以通过消除环来修正。更仔细的做法是将每条无向边拆为两条有向边，然后求边不相交的有向路径，所得结果即为无向图中的边不相交路径数。

== 7.7 最大流问题的扩展（Extensions to the Maximum-Flow Problem）

=== 带需求的循环流（Circulation with Demands）

#definition[
  一个*带需求的循环流（circulation with demands）*在每条边上仍保有容量 $c_e$，但每个节点 $v$ 有一个*需求（demand）* $d_v$：
  - 若 $d_v > 0$，表示 $v$ 是*消耗点（sink）*，净流入应为 $d_v$。
  - 若 $d_v < 0$，表示 $v$ 是*供给点（source）*，净流出应为 $|d_v|$。
  - 若 $d_v = 0$，表示普通中间节点。

  流 $f$ 需满足：
  - *容量约束*：$0 <= f_e <= c_e$。
  - *守恒约束（含需求）*：$f^"in"(v) - f^"out"(v) = d_v$，对所有 $v$。
]

#lemma(name: "可行性必要条件")[
  若存在可行循环流，则 $sum_v d_v = 0$（总供给等于总消耗）。
]

#proof[
  对所有节点的守恒等式求和，左侧流出减流入正负抵消，故 $sum_v d_v = 0$。
]

=== 建模为最大流

将带需求的循环流转化为标准最大流问题：

1. 添加*超源（super-source）* $s^*$ 和*超汇（super-sink）* $t^*$。
2. 对每个供给节点 $v$（$d_v < 0$），添加边 $(s^*, v)$，容量为 $-d_v$。
3. 对每个消耗节点 $v$（$d_v > 0$），添加边 $(v, t^*)$，容量为 $d_v$。
4. 原网络中的边保持不变。

#theorem[
  原网络存在可行循环流当且仅当上述最大流网络中从 $s^*$ 到 $t^*$ 的最大流饱和了所有从 $s^*$ 出发的边（即流值等于 $sum_{v: d_v > 0} d_v$）。
]

#proof[
  若存在可行循环流 $f$，则在扩展网络中定义 $f'(s^*, v) = -d_v$、$f'(v, t^*) = d_v$，其余边同 $f$，即得一个饱和所有 $s^*$ 出边的流。反之，若最大流饱和了所有 $s^*$ 出边，则限制在原边上的流即为满足所有节点需求的循环流。
]

=== 带下界的循环流（Circulation with Lower Bounds）

#definition[
  每条边 $e$ 不仅有上界 $c_e$，还有*下界（lower bound）* $ell_e$，要求 $ell_e <= f_e <= c_e$。需求为 $d_v$ 的循环流需同时满足下界和上界约束。
]

==== 转化为无下界问题

核心技巧：先将每条边推送 $ell_e$ 单位流量，然后将其转化为对节点需求的调整。

1. 初始化 $f_e = ell_e$。
2. 计算调整后的需求：$d_v' = d_v - (f^"in"(v) - f^"out"(v))$，其中 $f^"in"(v) - f^"out"(v)$ 是初始流 $ell$ 在 $v$ 处的净流入。
3. 将边 $e$ 的容量调整为 $c_e' = c_e - ell_e$（上界减少，下界变为 0）。
4. 在新网络上求解不带下界的可行循环流 $f'$。
5. 最终流为 $f_e + f_e'$。

=== 多源多汇问题

对有多个源点和多个汇点的网络，添加超源和超汇：超源连到每个源点（容量 $∞$），每个汇点连到超汇（容量 $∞$），转化为单源单汇的最大流问题。

== 7.8 调查设计（Survey Design）

=== 问题定义

某公司想对消费者进行产品调查。有 $n$ 个消费者和 $m$ 个产品。对每个消费者 $i$，需调查其在区间 $[c_i, c_i']$ 个产品；对每个产品 $j$，需被调查的消费者数量在 $[p_j, p_j']$ 之间。已知消费者 $i$ 愿意评价的产品集合 $S_i$。目标是设计一个满足所有约束的调查方案。

=== 循环流建模

1. 构造源点 $s$ 和汇点 $t$。
2. 左边节点代表消费者 $i$，右边节点代表产品 $j$。
3. 源点到消费者 $i$：下界 $c_i$、上界 $c_i'$。
4. 消费者 $i$ 到产品 $j$（若 $j in S_i$）：下界 0、上界 1。
5. 产品 $j$ 到汇点 $t$：下界 $p_j$、上界 $p_j'$。
6. 汇点 $t$ 到源点 $s$：下界 0、上界 $∞$（将最大流问题转化为循环流问题）。

在每条边 $(t, s)$ 上设置容量 $∞$，使得整个图成为一个循环流网络。加上需求 $d_v = 0$（所有节点守恒）。求带下界的可行循环流。若存在，则每条消费者-产品边上的流量 0 或 1 指示是否由该消费者评价该产品。

#figure(
  block(text(size: 0.9em)[
    网络结构：$s ->$ 消费者（$[c_i, c_i']$）$->$ 产品（$[0, 1]$）$-> t$（$[p_j, p_j']$），
    再加 $t -> s$（$[0, ∞]$）构成循环。
  ]),
  caption: [调查设计的循环流建模],
)

== 7.10 图像分割（Image Segmentation）

=== 问题定义

*图像分割*（image segmentation）是将图像中的每个像素分为*前景（foreground）*或*背景（background）*。给定：
- 每个像素 $v$ 属于前景的似然 $a_v >= 0$，属于背景的似然 $b_v >= 0$。
- 相邻像素 $u, v$ 之间的分离代价 $p_(u v) >= 0$（若 $u$ 和 $v$ 被分到不同集合，则需付出代价）。

目标是找到一个划分（前景集 $F$、背景集 $B$），最大化：
$ q(F) = sum_{v in F} a_v + sum_{v in B} b_v - sum_{(u,v) in E, |{u,v} inter F| = 1} p_(u v) $

=== 转化为最小割

最大化 $q(F)$ 等价于最小化：
$ sum_(v in B) a_v + sum_(v in F) b_v + sum_((u,v) in E, u "和" v "被分离") p_(u v) $
即：$a_v$ 中属于背景的部分 + $b_v$ 中属于前景的部分 + 被分离的相邻像素的 $p_(u v)$。

构造网络：
1. 源点 $s$ 代表前景，汇点 $t$ 代表背景。
2. 对每个像素 $v$：
   - 边 $(s, v)$：容量 $a_v$（若 $v$ 被分到背景 $B$，割断此边付出 $a_v$）。
   - 边 $(v, t)$：容量 $b_v$（若 $v$ 被分到前景 $F$，割断此边付出 $b_v$）。
3. 对每对相邻像素 $(u, v)$，添加双向边 $(u, v)$ 和 $(v, u)$，各容量 $p_(u v)$（若 $u$ 和 $v$ 分属不同集合，需割断其中一条，付出 $p_(u v)$）。

#theorem[
  在上述网络中，最小 s-t 割 $(A, B)$ 对应最优分割 $F = A \\ {s}$、$B = B \\ {t}$，且最小割容量等于最小化目标的值。
]

#proof[
  割的容量包含：从 $s$ 到 $B$ 的边的容量和（$sum_{v in B} a_v$），从 $A$ 到 $t$ 的边的容量和（$sum_{v in A} b_v$），以及所有从 $A$ 到 $B$ 的相邻边容量（$sum$ 被分离的相邻像素 $p_(u v)$）。这正是要最小化的表达式。
]

== 7.11 项目选择（Project Selection）

=== 问题定义

给定一组*项目（projects）* $P = {1, ..., n}$，每个项目 $i$ 有一个*收益（revenue）* $p_i$（可正可负，负收益即成本）。项目间存在*先决条件（prerequisites）*关系：若选择项目 $i$，则必须也选择项目 $j$，记作 $i -> j$。先决条件构成一个有向无环图（DAG）。目标是选择一个*可行*的项目子集 $A ⊆ P$（即若 $i in A$ 且 $i -> j$，则 $j in A$），最大化总收益 $sum_{i in A} p_i$。

=== 最小割建模

1. 构造源点 $s$ 和汇点 $t$。
2. 对每个项目 $i$：
   - 若 $p_i > 0$（正收益），加边 $(s, i)$，容量 $p_i$。
   - 若 $p_i < 0$（负收益，即成本），加边 $(i, t)$，容量 $-p_i$。
3. 对每条先决条件 $i -> j$，加边 $(i, j)$，容量 $∞$（确保若 $i$ 被选入 $A$ 而 $j$ 被选入 $B$，则此边贡献无限容量，使割不可能是最小割）。

#theorem(name: "项目选择的最优性")[
  最大收益 = $sum_(i: p_i > 0) p_i - "最小割容量"$。
  其中最小割 $(A, B)$ 对应项目选择：选择 $A \\ {s}$ 中的项目，放弃 $B \\ {t}$ 中的项目。
]

#proof[
  记 $P^+ = sum_{i: p_i > 0} p_i$ 为正收益总和。最小割 $(A, B)$ 的容量包含：
  - 从 $s$ 到 $B$ 的边（正收益项目 $i$ 被放弃，割断 $(s, i)$，付出 $p_i$）。
  - 从 $A$ 到 $t$ 的边（负收益项目 $i$ 被选择，割断 $(i, t)$，付出 $-p_i$）。
  - $∞$ 容量的先决条件边不可能被包含在最小割中，保证了 $A$ 的可行性（若 $i in A$ 但 $j in B$，则 $(i, j)$ 会贡献 $∞$，不可能最小）。

  选择 $A \\ {s}$ 的总收益为 $sum_(i in A) p_i = P^+ - ("放弃的正收益") - ("选择的负收益绝对值")$。这正好等于 $P^+ - "割容量"$。因此最大化收益等价于最小化割容量。
]

#figure(
  block(text(size: 0.9em)[
    网络结构：$s$ 连正收益项目（容量 $p_i$），负收益项目连 $t$（容量 $-p_i$），
    先决条件 $i -> j$ 对应容量 $∞$ 的边。
  ]),
  caption: [项目选择的最小割建模],
)

=== 应用解释

最小割将节点分为 $A$（与 $s$ 同侧）和 $B$（与 $t$ 同侧）。$A$ 中的项目（不含 $s$）即为最终选择。$∞$ 边确保选择集的封闭性（若选 $i$ 则必选其所有后继）。这种建模将"选择最大化"问题转化为"删除最小权值边集"问题，是最大流最小割的经典应用。

== 7.12 棒球淘汰（Baseball Elimination）

=== 问题定义

在棒球联赛中，共有 $n$ 支队伍。每支队伍 $i$ 已经赢了 $w_i$ 场比赛，还剩 $g_i$ 场比赛未打。剩余比赛中，队伍 $i$ 和 $j$ 之间还有 $g_(i j)$ 场比赛。问题是：判断某支队伍 $x$ 是否已被*数学上淘汰（eliminated）*，即是否存在一种剩余比赛的结果分配，使得 $x$ 最终排名第一（或并列第一）。

=== 最大流建模

要判断队伍 $x$ 是否可能夺冠，先计算其最大可能胜场数：$S = w_x + sum_j g_(x j)$（即 $x$ 赢下所有剩余比赛）。若存在某队伍 $i$ 满足 $w_i > S$，则 $x$ 已被淘汰。若不存在，仍需检查是否可能因其他队伍互相比赛导致某队超过 $S$。

构造流网络：
1. 源点 $s$ 和汇点 $t$。
2. 对每对队伍 $(i, j)$（$i, j ≠ x$，且 $i < j$，$g_(i j) > 0$），创建一个*比赛节点（game node）* $g_(i j)$。
3. 对每个队伍 $i ≠ x$，创建一个*队伍节点（team node）* $i$。
4. 从 $s$ 到每个比赛节点 $g_(i j)$ 加边，容量 $g_(i j)$（表示该比赛的总场次）。
5. 从比赛节点 $g_(i j)$ 到队伍节点 $i$ 和 $j$ 各加边，容量 $∞$。
6. 从每个队伍节点 $i$ 到 $t$ 加边，容量 $S - w_i$（表示队伍 $i$ 在不超过 $S$ 的前提下还能再赢的场次）。

#theorem(name: "棒球淘汰判定")[
  若从 $s$ 到 $t$ 的最大流值等于所有剩余比赛的总场次 $sum_(i < j, i,j ≠ x) g_(i j)$，则存在一种比赛结果使 $x$ 夺冠；否则 $x$ 已被淘汰。
]

#proof[
  若最大流饱和了所有从 $s$ 出发的边，则每条边 $g_(i j)$ 上的流量恰为 $g_(i j)$，表示分配给队伍 $i$ 和 $j$ 的胜场数之和等于该比赛总场次。队伍节点 $i$ 的净流入（即 $i$ 在剩余比赛中赢的场次）不超过 $S - w_i$，故 $i$ 的最终胜场不超过 $S$。反之，若最大流未饱和，则无论如何分配，必有某队最终超过 $S$。
]

=== 淘汰证书

若 $x$ 被淘汰，则最小割给出一个"淘汰证书"：存在一个队伍子集 $T ⊆ V \\ {x}$，使得这些队伍之间的剩余比赛场次之和大于它们允许超过 $x$ 的总胜场数。形式化地：

$ sum_(i,j in T, i < j) g_(i j) > sum_(i in T) (S - w_i) $

这意味着 $T$ 中的队伍无论怎样分配内部比赛结果，总有一支队伍最终胜数会超过 $S$。这种证书给人直观的理解：$x$ 并非被某单一队伍击败，而是被一群队伍的整体赛程所"围剿"。
