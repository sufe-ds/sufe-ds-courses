#import "util.typ": *

= 第一章 引言：若干代表性问题

本章从两个角度切入算法设计：一是通过*稳定匹配*（Stable Matching）问题展示从问题形式化到算法设计与分析的完整过程；二是概览五个代表性计算问题，展示问题难度的光谱——从多项式时间可解到 NP 完全乃至 PSPACE 完全。

== 1.1 第一个问题：稳定匹配（Stable Matching）

=== 问题背景与形式化定义

考虑 $n$ 个男性 $M = {m_1, ..., m_n}$ 与 $n$ 个女性 $W = {w_1, ..., w_n}$。每个男性对全体女性有一个严格的*偏好列表*（preference list），按偏好从高到低排序；女性亦然。

*定义。* 一个*匹配*（matching）是 $M times W$ 上的一个二元关系，满足每个人至多出现在一对中。若所有人均被配对，则称该匹配为*完美匹配*（perfect matching）。

*定义。* 给定一个完美匹配 $S$，若存在 $(m, w) !in S$ 使得 $m$ 偏好 $w$ 胜过其当前伴侣，且 $w$ 偏好 $m$ 胜过其当前伴侣，则称 $(m, w)$ 为一个*阻塞对*（blocking pair）。不含任何阻塞对的完美匹配称为*稳定匹配*（stable matching）。

=== Gale-Shapley 算法

Gale 与 Shapley（1962）提出了以下*延迟接受*（deferred acceptance）算法：

```python
def gale_shapley(M, W, man_pref, woman_pref):
    """Gale-Shapley 延迟接受算法"""
    # 初始化
    free_men = list(M)
    current_match = {w: None for w in W}
    next_proposal = {m: 0 for m in M}

    while free_men:
        m = free_men.pop(0)
        w = man_pref[m][next_proposal[m]]
        next_proposal[m] += 1

        if current_match[w] is None:
            current_match[w] = m
        else:
            m_prime = current_match[w]
            if woman_pref[w].index(m) < woman_pref[w].index(m_prime):
                current_match[w] = m
                free_men.append(m_prime)
            else:
                free_men.append(m)

    return current_match
```

=== 算法性质

*定理 1.1（终止性）。* 算法至多 $n^2$ 轮后终止。
*证明。* 每轮有一个自由男性向一位尚未求婚的女性求婚。至多有 $n times n$ 对不同的（男性, 女性）组合，故循环次数不超过 $n^2$。

*定理 1.2（完美匹配）。* 算法终止时产生的匹配是完美匹配。
*证明。* 假设终止时存在自由男性 $m$。由于 while 循环已停止，$m$ 必已向所有女性求过婚。因此每位女性至少被求婚过一次；一旦女性被求婚，她即进入订婚状态且此后始终保持订婚。故 $n$ 位女性均已订婚。订婚是一一对应的，所以 $n$ 位男性亦均已订婚，与 $m$ 自由矛盾。故无自由男性。由订婚数相等，全体女性亦订婚，得到完美匹配。

*定理 1.3（稳定性）。* 算法返回的匹配是稳定匹配。
*证明。* 反证。设返回匹配 $S$ 中含阻塞对 $(m, w)$。记 $m$ 在 $S$ 中的伴侣为 $w'$，$w$ 在 $S$ 中的伴侣为 $m'$。故 $m$ 偏好 $w$ 胜过 $w'$，$w$ 偏好 $m$ 胜过 $m'$。

因 $m$ 按偏好降序求婚，他必先向 $w$ 求婚，后向 $w'$ 求婚。考虑 $m$ 向 $w$ 求婚的时刻：

- 若 $w$ 当时自由，则她接受 $m$。此后 $w$ 的伴侣只会不断改善，故其最终伴侣 $m'$ 不劣于 $m$，与 $w$ 偏好 $m$ 胜过 $m'$ 矛盾。
- 若 $w$ 当时已与某人订婚，则她或接受 $m$（若偏好 $m$ 胜过现任），或拒绝 $m$（若偏好现任胜过 $m$）。无论哪种情形，$w$ 的最终伴侣均不劣于 $m$，矛盾。

故不存在阻塞对，$S$ 稳定。

=== 男性最优性与女性最劣性

*定义。* 若存在某个稳定匹配将 $m$ 与 $w$ 配对，则称 $w$ 是 $m$ 的*合法伴侣*（valid partner）。

*定理 1.4（男性最优性，Man-Optimality）。* 在 Gale-Shapley 算法中，每位男性 $m$ 得到其所有合法伴侣中排名最高的女性。
*证明。* 先证核心引理：算法执行过程中，没有男性被合法伴侣拒绝。

假设不然。设 $m$ 是第一个被合法伴侣拒绝的男性，被 $w$ 拒绝（$w$ 选择了 $m'$）。故 $w$ 偏好 $m'$ 胜过 $m$，且存在稳定匹配 $S$ 含 $(m, w)$。

在 $S$ 中 $m'$ 与 $w'$ 配对。由稳定性，$m'$ 必须偏好 $w'$ 胜过 $w$（否则 $(m', w)$ 阻塞 $S$）。算法中 $m'$ 按偏好降序求婚，故先向 $w'$ 求婚。

若 $w'$ 当时拒绝 $m'$，则 $m'$ 已被合法伴侣 $w'$ 拒绝在先，矛盾。若 $w'$ 接受 $m'$，则 $m'$ 与 $w'$ 订婚；但 $m'$ 最终与 $w$ 配对，故 $w'$ 其后必因遇到更优人选而抛弃 $m'$。于是 $m'$ 再次被合法伴侣拒绝在先，矛盾。

引理得证。因男性依偏好降序求婚，且从未失去合法伴侣，每位男性必得到其偏好列表上排名最高的合法伴侣。

*定理 1.5（女性最劣性，Woman-Pessimality）。* 在 Gale-Shapley 算法中，每位女性 $w$ 得到其所有稳定伴侣中排名最低的男性。
*证明。* 设 $S$ 为任意稳定匹配。记算法输出 $M_("GS")$ 中 $w$ 的伴侣为 $m$，$S$ 中为 $m'$。假设 $w$ 偏好 $m'$ 胜过 $m$（即 $m$ 不是最差的），导出矛盾。

由男性最优性，$m$ 偏好其在 $M_("GS")$ 中的伴侣 $w$ 胜过其在 $S$ 中的伴侣 $w'$。于是 $m$ 偏好 $w$ 胜过 $w'$，$w$ 偏好 $m'$ 胜过 $m$，则 $(m, w)$ 阻塞 $S$，矛盾。故 $w$ 不可能偏好 $m'$ 胜过 $m$，即 $m$ 是 $w$ 所有稳定伴侣中最差的。

=== 扩展：居民与医院匹配（Hospital-Resident Matching）

稳定匹配问题在现实中的重要应用是*居民/医院匹配*（Hospital-Resident, HR）。在该模型中，医院有多个名额（capacity），居民对医院有偏好排序，医院对居民亦有偏好排序。目标是产生一个稳定匹配：不存在一位居民与一家医院组成的阻塞对，即该居民偏好这家医院胜过其当前分配，且该医院仍有空额或偏好这位居民胜过某位已分配居民。

Gale-Shapley 算法可经适应性调整后解决此问题（由医院或居民作为提议方，配合名额约束）。该框架被广泛应用于美国国家住院医师匹配计划（NRMP）等现实场景。

== 1.2 五个代表性问题（Five Representative Problems）

本节介绍五个经典计算问题。它们均可视为*独立集*（Independent Set）问题在图的不同子类上的变体，但其计算复杂度跨越从多项式到指数级的广泛光谱。

=== 1.2.1 区间调度（Interval Scheduling）

*问题定义。* 给定 $n$ 个区间 $(s_i, f_i)$（$s_i$ 为开始时间，$f_i$ 为结束时间），选择尽可能多的互不重叠的区间。

该问题等价于在*区间图*（interval graph）上求解独立集。区间图是弦图的一种，其上独立集可通过*贪心算法*（greedy algorithm）高效求解：按结束时间升序排序，依次选取与已选区间不冲突的区间。

$->$ 贪心算法（第 4 章）

=== 1.2.2 加权区间调度（Weighted Interval Scheduling）

*问题定义。* 给定 $n$ 个区间，每个区间附带权值 $v_i >= 0$，选择一组互不重叠的区间使得权值之和最大。

引入权值后，简单的贪心策略不再奏效。该问题需用*动态规划*（Dynamic Programming）在 $O(n op("log") n)$ 时间内求解：按结束时间排序后，定义 $"OPT"(j)$ 为前 $j$ 个区间中的最优解，有递推关系

$ "OPT"(j) = max(v_j + "OPT"(p(j)), "OPT"(j-1)) $

其中 $p(j)$ 为与区间 $j$ 不冲突的最右区间。

$->$ 动态规划（第 6 章）

=== 1.2.3 二部图匹配（Bipartite Matching）

*问题定义。* 给定二部图 $G = (X ∪ Y, E)$，找出最大的边集 $M ⊆ E$，使得 $M$ 中任意两条边没有公共端点。

二部图匹配可看作稳定匹配问题在不考虑偏好时的简化版本。该问题可通过*增广路*（augmenting path）方法或规约为*最大流*（Maximum Flow）问题求解，多项式时间内可解。

$->$ 网络流（第 7 章）

=== 1.2.4 独立集（Independent Set）

*问题定义。* 给定无向图 $G = (V, E)$，找出最大的顶点集 $S ⊆ V$，使得 $S$ 中任意两个顶点不相邻。

在一般图上，独立集问题是 *NP 完全*（NP-Complete）的——除非 P = NP，否则不存在多项式时间算法。前三个问题本质上都是在特殊图类上求解独立集：区间调度对应区间图，加权区间调度对应带权区间图，二部图匹配可转化为某种线图（line graph）的独立集。正是这些特殊结构使问题变得容易处理。

$->$ NP 完全性理论（第 8 章）

=== 1.2.5 竞争性设施选址（Competitive Facility Location）

*问题定义。* 两个玩家轮流在图 $G$ 上放置设施，规则是不能放置在已有设施所在或相邻的顶点上。每个顶点有非负权值。给定目标值 $k$，判定先手玩家能否通过最优博弈实现总权值至少 $k$。

该问题是*独立集*的双人博弈版本。它属于 *PSPACE 完全*（PSPACE-Complete）——在多项式空间可解的问题中最难的一类。一般认为 PSPACE 完全问题不存在多项式时间算法。

$->$ PSPACE 完全性（第 9 章）

=== 小结

下表概括五个代表性问题及其核心特征：

#table(
  columns: 5,
  align: (left, left, left, left, center),
  table.header([问题], [图类 / 结构], [算法 / 技术], [难度类别], [章节]),
  [区间调度], [区间图], [贪心], [$O(n op("log") n)$], [4],
  [加权区间调度], [区间图 + 权值], [动态规划], [$O(n op("log") n)$], [6],
  [二部图匹配], [二部图], [增广路 / 最大流], [多项式时间], [7],
  [独立集], [一般图], [—], [NP 完全], [8],
  [竞争性设施选址], [一般图 + 博弈], [—], [PSPACE 完全], [9],
)
