# T3

编译命令：`g++ T3.cpp -o T3.exe -O2 -Wall -Wshadow`

我一开始想要通过 `UTF-8` 实现中英文标点检测，吭呲吭呲写了一大堆之后意识到：用户通过终端输入的可能也是 `GBK`。

然后我就放弃了。

边际成本太大了，这一次作业加的分不支持我去实现一个 `i18n` 支持特别好的字符串。

其实也可以用 `boost/local`，但是我觉得在这门课用第三方库反而会扣分。

就这样吧。
