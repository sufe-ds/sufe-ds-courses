#set text(font: "LXGW WenKai Mono GB")
#set par(justify: true)

== 第一部分

1. I/O设备的编制方式有几种？说明它们的特点
  - *统一编址*：
    - *特点*：将I/O端口视为内存单元的一部分，使用相同的地址空间。指令系统中不设置专用的I/O指令，而是使用访存指令（如LOAD/STORE）访问I/O端口。
    - *优点*：指令功能强，编程灵活；外设数目几乎不受限制。
    - *缺点*：I/O端口占用部分主存空间，减少可用内存；访存指令较长，执行速度较慢；I/O地址译码电路复杂。
  - *不统一编址*（独立编址）：
    - *特点*：I/O端口有独立的地址空间，使用专门的I/O指令（如IN/OUT）访问。
    - *优点*：I/O端口不占用内存地址空间；地址线少，译码简单；I/O指令短，执行速度快。
    - *缺点*：I/O指令类型少，程序设计灵活性差；需要额外的控制信号（如IOR/IOW），增加控制逻辑复杂性。

2. I/O系统与主机交换信息有几种控制方式？各有何特点？
  - *程序查询方式*：
    - *特点*：CPU主动查询I/O设备状态，通过循环检查设备是否准备就绪。CPU与I/O串行工作，有“踏步等待”现象。
    - *优点*：控制简单，易于实现。
    - *缺点*：CPU效率低，无法并行处理其他任务。
  - *程序中断方式*：
    - *特点*：I/O设备准备就绪后向CPU发出中断请求，CPU暂停当前程序，转去执行中断服务程序。CPU与I/O并行工作。
    - *优点*：消除了踏步等待，提高了CPU利用率。
    - *缺点*：中断处理需要时间，频繁中断可能影响性能。
  - *DMA方式*（直接存储器存取）：
    - *特点*：通过DMA控制器直接在I/O设备和主存之间传输数据，无需CPU介入。采用“周期挪用”方式窃取CPU访存周期。
    - *优点*：高速数据传输，CPU与I/O完全并行；减少了CPU开销。
    - *缺点*：需要额外的DMA控制器硬件；控制逻辑复杂。

== 第二部分
1. 为了反映外围设备的工作状态，在I/O接口中都设有状态触发器。常见有\_\_\_\_\_\_、\_\_\_\_\_\_、\_\_\_\_\_\_、和\_\_\_\_\_\_。
  - *准备就绪触发器*（Ready）：表示设备已准备就绪，可进行数据交换。
  - *忙触发器*（Busy）：表示设备正忙于工作，无法接收新命令。
  - *中断请求触发器*（INTR）：表示设备向CPU发出中断请求。
  - *中断屏蔽触发器*（MASK）：用于屏蔽设备的中断请求。
  - （注：幻灯片中还提到了中断允许触发器（EINT），用于控制CPU是否响应中断。）

2. 简述接口的功能和组成
  - 接口的功能：
    - 设备选择：通过地址译码识别特定设备。
    - 数据缓冲：缓冲数据，解决CPU与I/O设备之间的速度差异。
    - 数据格式转换：实现串行/并行数据转换。
    - 电平转换：适配不同设备的电平标准。
    - 传送控制命令：接收并执行CPU的命令。
    - 反映设备状态：通过状态触发器指示设备工作状态（如Ready、Busy）。
    - 中断管理：处理中断请求和屏蔽。
  - 接口的组成：
    - 设备选择电路：译码设备地址。
    - 命令寄存器/译码器：存储和解析CPU命令。
    - 数据缓冲寄存器（DBR）：暂存传输数据。
    - 设备状态触发器：如Ready、Busy、INTR、MASK等。
    - 控制逻辑电路：协调接口各部分工作。
    - 中断机构：处理中断请求和优先级排队。

== 第三部分

1. 一次中断处理过程可分为\_\_\_\_\_\_、\_\_\_\_\_\_、\_\_\_\_\_\_、\_\_\_\_\_\_和\_\_\_\_\_\_五个过程
  - 中断请求：I/O设备向CPU发出中断请求。
  - 中断响应：CPU在条件满足时响应中断。
  - 保护现场：保存程序断点和寄存器内容。
  - 中断服务：执行中断服务程序，处理I/O操作。
  - 恢复现场和中断返回：恢复保存的上下文，返回原程序。

2. CPU响应中断要保护现场，包括\_\_\_\_\_\_和\_\_\_\_\_\_保护。前者通过\_\_\_\_\_\_实现，后者可通过\_\_\_\_\_\_实现
  - 程序断点（当前指令地址）：通过中断隐指令自动压栈实现。
  - 寄存器内容（如ACC、状态寄存器）：通过软件指令（如PUSH指令）压栈实现。

3. 采用硬件向量法形成中断服务程序入口地址完成后，CPU在中断周期内还须完成\_\_\_\_\_\_、\_\_\_\_\_\_和\_\_\_\_\_\_操作
  - 程序断点进栈：保存返回地址。
  - 关中断：防止其他中断干扰。
  - 向量地址送PC：将中断向量地址写入程序计数器，跳转到中断服务程序入口。

4. 简述CPU响应中断的条件和时间
  - 响应条件：
    - 允许中断触发器EINT=1（CPU处于开中断状态）。
    - 有中断请求（INTR=1）且未被屏蔽（MASK=0）。
    - 当前指令执行结束（除少数长指令外）。
  - 响应时间：
    - 通常在每条指令执行阶段的结束前，CPU发送中断查询信号，检测中断请求。若条件满足，则进入中断周期。

5. 简述单重中断和多重中断的处理流程，说明它们的异同之处
  - 单重中断处理流程：
    - 中断响应后立即关中断。
    - 保护现场（程序断点和寄存器）。
    - 执行中断服务程序。
    - 恢复现场。
    - 开中断并中断返回。
    - *特点*：不允许中断嵌套，整个服务过程不可被打断。
  - 多重中断处理流程：
    - 中断响应后可能保持开中断（或仅在关键部分关中断）。
    - 保护现场后开中断，允许更高优先级中断。
    - 执行中断服务程序（可能被更高优先级中断打断）。
    - 关中断后恢复现场。
    - 开中断并中断返回。
    - *特点*：允许中断嵌套，提高系统实时性。
  - 异同比较：
    - 相同点：都包含中断响应、保护现场、中断服务、恢复现场和返回的基本流程。
    - 不同点：
      - 单重中断：全程关中断，无法处理更高优先级中断。
      - 多重中断：在保护现场后开中断，允许优先级更高的中断嵌套。

== 第四部分

1. 中断向量地址是什么？
  - *答案*：C. 中断服务程序入口地址的地址
  - *解释*：中断向量地址是中断向量表中的索引地址，指向中断服务程序入口地址的存储位置。当发生中断时，CPU通过中断向量地址找到中断服务程序的入口地址，从而执行相应的中断处理程序。

2. 在单重中断系统中，CPU一旦响应中断，则立即将什么触发器置为"0"，以防本次中断服务结束前同级的其他中断源产生另一次中断进行干扰？
  - *答案*：B. 中断允许
  - *解释*：在单重中断系统中，CPU响应中断后，会立即将中断允许触发器（EINT）置为"0"，即关闭中断，以防止当前中断服务程序执行期间被同级的其他中断源打断。确保中断服务程序执行完毕后再重新开放中断。

3. 下面哪些情况下会提出中断请求？
  - *答案*：A、C、D
  - *解释*：
    - *A. 用户输入时按下一个键*：键盘输入会触发中断请求，通知CPU处理输入数据。
    - *B. 两数相加等于零*：这属于算术运算结果，通常不会直接触发中断，除非有特定监控机制（如溢出检测），但一般不会产生中断请求。
    - *C. DMA接口传送一批数据结束*：DMA传输完成后会向CPU发出中断请求，进行后处理（如校验数据）。
    - *D. 单步调试C程序*：单步调试利用陷阱中断（如单步中断），每条指令执行后都会触发中断，便于程序调试。

4. DMA接口主要包括哪些部件组成？它们有何功能？
  - *地址寄存器（AR）*：存储主存中数据的起始地址，在传输过程中自动增量或减量，以指向下一个数据位置。
  - *字计数器（WC）*：记录待传输数据的数量，每传输一个数据，计数器减1，当计数器为0时发出传输完成信号。
  - *数据缓冲寄存器（DBR）*：临时存储从设备或主存传输的数据，实现速度匹配。
  - *DMA控制逻辑*：管理DMA传输过程，包括生成控制信号、协调数据传输和处理总线控制权的转交。
  - *中断机构*：在DMA传输完成后，向CPU发出中断请求，进行后处理（如错误检查）。
  - *功能总结*：DMA接口允许外设直接与主存交换数据，无需CPU频繁干预，提高数据传输效率。

5. 什么是周期挪用？周期挪用可能会出现几种情况？
  - *周期挪用*：是DMA传输的一种方式，DMA控制器在CPU不访问主存时"窃取"总线周期进行数据传输，或在CPU与DMA同时请求访存时，DMA优先使用总线。
  - *可能出现的三种情况*：
    - *CPU不访问主存*：DMA直接使用总线周期进行数据传输。
    - *CPU正在访问主存*：DMA等待当前总线周期结束后再使用总线。
    - *CPU与DMA同时请求访存*：DMA优先使用总线（周期挪用），CPU暂时等待。

6. 试比较程序中断和DMA方式有何区别？
  - *数据传送控制*：
    - 程序中断方式：由CPU执行中断服务程序实现数据传输。
    - DMA方式：由DMA控制器直接控制数据传输。
  - *CPU干预*：
    - 程序中断方式：每次传输都需要CPU参与。
    - DMA方式：传输过程无需CPU参与，仅后处理需要。
  - *响应时间*：
    - 程序中断方式：在指令执行结束后响应。
    - DMA方式：在总线周期结束时响应。
  - *处理异常*：
    - 程序中断方式：能处理异常事件（如除零错误）。
    - DMA方式：仅负责数据传输，不能处理异常。
  - *中断请求*：
    - 程序中断方式：用于数据传输和后处理。
    - DMA方式：仅在传输完成后发出中断请求进行后处理。
  - *优先级*：
    - 程序中断方式：优先级较低。
    - DMA方式：优先级较高（因需及时传输数据）。
  - *适用场景*：
    - 程序中断方式：低速设备或少量数据传输。
    - DMA方式：高速设备或大批量数据传输。
  - *总结*：DMA方式适用于高速数据传送，减少CPU开销；程序中断方式适用于事件驱动处理，灵活性高。
